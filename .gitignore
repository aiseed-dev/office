target/
vendor/

# 機械ごとの設定。絶対パスや symlink が入るので追跡しない
.cargo/
.linklibs/
# miniforge の仮想環境(pandas 連携の検証用)
.venv/

# 手元に残すが、公開するものではない
core/
demo.html
index.html
logo.svg

# 検証で出るもの
*.pdf
lang/corpus/txt/

# 開いている印(排他ロック)。閉じれば消える — 決して追跡しない
.~lock.*

# 本文のフォントは同梱しない(システムから探す)
assets/
