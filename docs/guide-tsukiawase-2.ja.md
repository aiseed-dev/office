# 本家ガイド突き合わせ 第2便の全量(2026-08-06 ワークフロー調査)

ONLYOFFICE 公式ガイドの残り約50節を、テーマ16組×Sonnet の調査+反証検証で
calc と突き合わせた結果の**全量**。要約と消し込みの順番は SEKKEI.md の
「本家ガイドとの突き合わせ点検」を見よ。ここは在庫台帳 — 直したら行頭に ✔ を付ける。

集計: **確定 97 件**(大15・中48・小34)/ 訂正・方針 59 件 / 一致 37 件

## 確定した食い違い(組ごと、重い順)

### 基本操作(2件)

- ✔(第1版 2026-08-07: CSV 書き出し=ファイル頁の釦と「名前を付けて保存」の絞り(UTF-8 BOM+CRLF・値だけ・入らない物は文言で言う)。PDF は従来から印刷で。ODS/OTS/XLTX/PDF-A は控え — 別の書式系を1から書く大物で、xlsx と PDF と CSV で実務は回るため後回し)**[中] 保存形式の選択肢(XLSX, XLTX, ODS, OTS, CSV, PDF, PDFA)**(OpenCreateNew.aspx)
  - 本家: 「名前を付けて保存」やダウンロード時にXLSX・XLTX・ODS・OTS・CSV・PDF・PDFAの中から形式を選べる。
  - calc: calc/src/io.rs:416-419のsave_asはadd_filter("Excelブック", ["xlsx"])のみ。PDF書き出しは別コマンドとして存在する(io.rs:494-496)。ODS/OTS/XLTX/CSV/PDFAでの保存はio.rs・cmds.rsをgrepしても見当たらず、SEKKEI.mdにも当該形式への言及は無い。
- 割り切り(2026-08-07: 作らない、と決めた。calc の設計は「他のブックは**値として**取り込む — リンクを張らない=リンク切れ・古びた参照の帳票を作らない」(cmds.rs data-external-links、SEKKEI の分業)。=[Book1.xlsx]Sheet1!A1 の生きた外部参照はこの設計と衝突するため見送り。必要なら データタブ「外部データ」で値の取り込み直し)**[小] 数式入力中に別ワークブックのセルをクリックして外部参照式(=[Book1.xlsx]Sheet1!A1 形式)を作る**(AddExternalLinks.aspx)
  - 本家: ソース・宛先の両ブックを開いた状態で数式入力を開始し、別ウィンドウのセルを左クリックすると外部参照付きの数式が自動生成される。
  - calc: calcは取り込み時にformulaをNoneにして値化するため(cmds.rs:1607-1609)、他ブックへの数式参照を作る経路がそもそも無い。sheet::Sheet.linksフィールド(picks.rs:1312,1317)はハイパーリンク(URL等)の管理で本機能とは別物。

### 文字の書式(6件)

- ✔ **[中] 大文字小文字変更のオプション種類**(FontTypeSizeStyle.aspx)
  - 本家: Sentence case / lowercase / UPPERCASE / Capitalize Each Word / tOGGLE cASE の5択サブメニュー
  - calc: calc/src/cmds.rs:248-287 の "changecase" はワンクリックでUPPER⇄lowerをトグルするのみ(has_lower 判定で全て大文字化 or 全て小文字化の二択)。picks.rs にサブメニュー項目も無い(grepで該当なし)。Capitalize Each Word 等は無い
- **[中] フォントカラー・塗りつぶし色の詳細指定(スポイト・RGB/16進入力・その他の色)**(FontTypeSizeStyle.aspx)
  - 本家: テーマカラー/標準カラーからの選択に加え、スポイトツールや「その他の色」でRGB値・16進コードを直接入力してカスタムカラーを設定できる
  - calc: calc/src/cmds.rs:1897-1912 の "fillcolor"/"fontcolor" は FILL_COLORS / FONT_COLORS という固定パレットからの選択のみ(calc/src/util.rs側で定義)。スポイト機能、RGB/16進コード直接入力、「その他の色」ダイアログは見当たらない(grepで該当コード無し)
- ✔(2026-08-07: URL の板に続けて表示テキストの板が開きセルへ入る。#シート名!B5 の形で帳面の中への参照になり Ctrl+クリックで跳ぶ(xlsx は location 属性で往復 — 従来は読み捨てだった)。スクリーンチップ欄は板1枚の作りに合わないため見送り)**[中] リンク設定ダイアログの構成(外部リンク/内部データ範囲の切替、表示テキスト欄、スクリーンチップテキスト欄)**(AddHyperlinks.aspx)
  - 本家: URL入力欄に加え「表示」(クリック可能テキスト)欄と「スクリーンチップテキスト」欄がある。内部データ範囲を選ぶとシート・セル範囲・名前付き範囲から選択するUIに切り替わる
  - calc: calc/src/cmds.rs:1837-1841 の "inshyperlink" は self.prompt = ("link", Editor::new(&cur)) という単一のテキスト入力欄(URL文字列のみ)を出すだけ。calc/src/picks.rs:1309-1322 の finish_prompt "link" も同様に文字列1つを links マップへ格納するのみで、表示テキスト・スクリーンチップ・内部シート参照選択・名前付き範囲選択のUIは無い
- ✔ **[小] クリアの選択肢**(ClearFormatting.aspx)
  - 本家: 「すべて」「テキスト」「書式」「コメント」「リンク」の5択
  - calc: calc/src/picks.rs:381-385 の "clr" サブメニューは「すべて」「テキスト(書式は残す)」「書式(中身は残す)」の3択のみ。コメント単独削除・リンク単独削除の選択肢が無い(cmds.rs の clear_range / clear は全体一括に対応する形跡のみ)
- ✔(2026-08-07: ホームの貼り付けの隣に刷毛 — いまのセルの書式を持ち、次に押したセル(Shift で範囲)に塗る。Esc でやめる)**[小] 書式のコピー(刷毛)**(ClearFormatting.aspx)
  - 本家: 書式元セル選択→アイコンでシングル/ダブルクリック→対象へ適用、Escで終了
  - calc: 既知(報告不要): 書式のコピー(刷毛)は無い
- ✔(2026-08-07)**[小] Ctrl+K ショートカットでリンク追加**(AddHyperlinks.aspx)
  - 本家: Ctrl+K でリンク設定ウィンドウを開ける
  - calc: main.rs内で "Ctrl+K" や対応するキーコードとリンクコマンドの結びつきが見当たらない(grepで該当なし)。挿入タブのアイコン、または右クリックメニュー経由の "hyperlink"(picks.rs:347-350)からのみ到達可能と見られる

### セルのプロパティ(8件)

- ✔(2026-08-07: 結合の釦が4択に — 結合して中央/横方向(行ごと)/結合だけ(揃えは触らない)/解除。値が複数のときの確認は共通)**[中] 結合の3種類(結合して中央揃え/横方向に結合/セルを結合)の区別とドロップダウンメニュー**(MergeCells.aspx)
  - 本家: ホームタブの結合アイコン横の矢印を開くと「結合して中央揃え」「横方向に結合」「セルを結合」の3択が出て、それぞれ結果のセル配置・結合の粒度が異なる。
  - calc: ribbon.rs:246 の c("merge", ...) は単一釦で、cmds.rs:151 で merge_selection() を直接呼ぶだけ。main.rs:2100-2121 の merge_selection() は選択範囲全体を1つに結合するトグル動作のみで、行ごとに結合する「横方向に結合」や、中央揃えを伴わない「セルを結合」に相当する分岐が無い。ドロップダウン(サブメニュー)自体が存在しない。
- ✔(2026-08-07: 10/12 — 外枠/格子/上/下/左/右/なし+内側の縦・横。斜めは Borders のモデルに無いので控え)**[中] 枠線の12種テンプレート(外枠/全枠/上/下/左/右/枠なし/内枠/内側縦/内側横/斜め上枠/斜め下枠)**(AddBorders.aspx)
  - 本家: 枠線ドロップダウンに12種のテンプレートがあり、斜め線を含む多様な組み合わせを1クリックで適用できる。
  - calc: picks.rs:1351-1357 に b-all(全枠)/b-out(外枠、border_outline() で range の外周のみ top/bottom/left/right を立てる)/b-none(枠なし)の3種のみ確認。内枠・内側縦横・上下左右個別・斜め線に対応する id や分岐は grep で見当たらない。Borders 構造体自体(sheet/src/model.rs:96-105)が top/bottom/left/right の4bool のみで斜め線のフィールドが無い。
- ✔(2026-08-07: Borders を辺ごとの Edge{線種13種・色}に拡張。styles.xml と往復、画面は太さ/破線/二重線/色で描き、紙(PDF)は太さと色。罫線▼の一覧=辺7種+線のスタイル12種+線の色(パレット+RGB直指定))**[中] 罫線のスタイル(線の太さ・線種:実線/破線/二重線など)と色の指定**(AddBorders.aspx)
  - 本家: 右サイドバーの「枠線スタイル」で線の太さ・線種・色を選んでから適用できる。
  - calc: sheet/src/model.rs:96 の Borders は有無のみの bool 4つで、太さ・線種・色を保持するフィールドが無い。xlsx 側(styles.rs)は読み書きで style/color を経由するが、calc の CellFormat.borders には反映されない。
- **[中] 塗りつぶしのグラデーション・パターン(前景色/背景色/パターン柄)**(AddBorders.aspx)
  - 本家: 右サイドバーの塗りつぶしで単色以外にグラデーション(角度・方向・複数ポイント)、パターン(柄+前景/背景色)を選べる。
  - calc: grep で "gradient"/"パターン"/"グラデーション" の実装該当が xlsx.rs/styles.rs の読み書き(patternFill の fgColor 読取り程度)のみで、calc 側に UI から単色以外の塗りを設定する経路が無い。fillparag は単色のみ(cmds.rs 内の色パレット経由と推測、grep で gradient 関連の cmd id 無し)。
- **[中] テキストの角度指定(反時計回り/時計回り/垂直/上に回転/下に回転の5パターン、および度単位の角度フィールド)**(AlignText.aspx)
  - 本家: 方向アイコンのメニューに5つの定型回転パターンがあり、右サイドバーで角度を度単位で細かく指定できる。
  - calc: cmds.rs:813-823 の text-orient はクリックのたびに rotation を巡回させる実装(match f.rotation)のみで、5パターンの選択メニューや度単位の任意角度入力フィールドは見当たらない(grep で「角度」・度単位の editable フィールド無し)。
- ✔(会計・分数は描けないので控えのまま)**[中] 数値形式の全種類(一般/数値/指数/会計/通貨/短い日付/長い日付/時刻/パーセント/分数/テキスト/その他/カスタム)を選ぶドロップダウン一覧**(ChangeNumberFormat.aspx)
  - 本家: ホームタブに数値形式ドロップダウンがあり、上記の12種+カスタムを選べる。
  - calc: ribbon.rs・cmds.rs・picks.rs を通覧した限り、numfmt サブメニュー(picks.rs:406-412)は「桁区切り」「通貨」「パーセント」「小数を増やす/減らす」の5項目のみ。指数・会計・分数・短い日付/長い日付/時刻の切替・テキスト形式・カスタム入力ダイアログへの導線が見当たらない(util.rs:824-825 のパレット定数も通貨・パーセントの2つのみ)。
- ✔(コード直打ちの板。プリセット組み立てUIは控え)**[中] カスタム数値形式ダイアログ(小数点桁数・1000区切り・通貨記号・負の値表示・日付/時刻/分数形式の選択)**(ChangeNumberFormat.aspx)
  - 本家: 「数値形式」ウィンドウでコード直接入力や各種プリセットから書式をカスタマイズできる。
  - calc: grep で「カスタム」・書式コード入力用の編集板が picks.rs / view.rs に見当たらず、fmt_panel(picks.rs:1348 以降)の中身も borders/fill 中心で numfmt のカスタム入力欄は確認できない。number_format は Option<String> で xlsx コードをそのまま保持できる型だが、calc の UI から任意コードを打ち込む経路が無い。
- ✔ **[小] 「結合して中央揃え」実行時にテキストが自動で中央揃えになる挙動**(MergeCells.aspx)
  - 本家: 結合して中央揃えを行うと、結合セルの内容が自動的に中央揃え(横・縦とも)になる。
  - calc: main.rs:2100-2121 の merge_selection() は sh.merges に範囲を積むだけで、align/valign を書き換える処理が無い(grep でも fmt.align への代入は見当たらない)。結合しても既存の左揃え等がそのまま残る。

### セルと行列の管理(4件)

- **[中] ホームタブの「セルを挿入」「セルを削除」アイコン**(add-cells.aspx / InsertDeleteCells.aspx)
  - 本家: ホームタブのアイコンからも右クリックと同じ「セルを右/下にシフト」等の選択が行えるはず(ガイドは「ツールバーのセル挿入アイコン→『セルを右にシフト』を選択」と明記)
  - calc: ui/src/ribbon.rs:262-263 で cell-ins/cell-del は c()=実装済みだが、calc/src/cmds.rs:113-114 では `"cell-ins" => self.rowcol(|s, p| s.insert_row(p.row))` `"cell-del" => ... remove_row` と、常に行の挿入・削除に固定されている。シフト方向の選択肢を出さず、列全体挿入や『セルを右にシフト』を選ぶ手段がツールバー側には無い(右クリックメニューでのみ選択可能)
- ✔ **[中] 「最上行の固定」「最初の列の固定」の個別プリセット選択(ドロップダウンメニュー)**(Freeze-rows-and-columns.aspx)
  - 本家: 表示タブの「ウィンドウ枠の固定」はドロップダウンで『最上行の固定』『最初の列の固定』を個別に選べる。行だけ・列だけの固定も可能
  - calc: calc/src/cmds.rs:1876-1888 の freeze はカーソル位置(行・列を同時に)固定するトグルのみで、行のみ/列のみを分けて固定するプリセット選択肢は無い。ドロップダウンメニューでもない(単純トグル)
- ✔(2026-08-07: 見出しの右クリックに「列の幅…」「行の高さ…」— 0〜255 /「0」何個ぶん・0〜409pt の範囲検査つき。選んだ列・行ぶんに掛かり、空 Enter で既定に戻す)**[小] 列幅0〜255、行高さ0〜408.75の数値指定ダイアログでの範囲**(InsertDeleteCells.aspx)
  - 本家: 列幅・行高さを数値ダイアログで指定でき、範囲が0〜255／0〜408.75に制限される
  - calc: calc/src/main.rs:676 付近でドラッグによる col_width 変更のみを確認。数値入力ダイアログや範囲チェックのコードは picks.rs / cmds.rs に見当たらない
- ✔(2026-08-07: 固定の境目をいつも描く — 影あり(表示タブの既存トグル)=開始位置にひかえめな緑の線+薄い影の帯、影なし=灰色の1px線。本家の描き分けと同じ)**[小] 固定枠の「影を表示」チェックボックスと、影無効時の灰色の線/開始位置の緑の線の描き分け**(Freeze-rows-and-columns.aspx)
  - 本家: 固定開始位置に微妙な緑の線、影表示チェックボックスのON/OFFで見た目が変わる(影表示OFF時は灰色の線)
  - calc: calc/src/view.rs 内に frozen の境界線描画はあるが(view.rs:1322は「枠の固定」というメニュー文言のみ)、影表示の有無を切り替えるチェックボックスや緑/灰の描き分けのコードは見当たらない

### データ処理(7件)

- ✔(聞き取りの板で複数基準。色基準・大文字小文字は控え)**[大] 複数列のカスタムソート・ダイアログ**(SortData.aspx)
  - 本家: 「並べ替え」ダイアログで複数の基準行を追加(新規/コピー/削除、上下移動)、各基準を値・セルの色・フォントの色から選び、昇順/降順(A-Z/Z-A)を指定、「オプション」ボタンで大文字小文字区別等を設定できる。
  - calc: calc/src/cmds.rs:1915 の "custom-sort" は self.cursor.col の1列を対象に sort_by_column(c, true, true) を呼ぶだけで、複数基準・色基準・昇順降順の選択・ダイアログは無い。sheet/src/model.rs:1491 sort_by_column も列番号1本と ascending 真偽・header 真偽のみを引数に取る単純実装。
- ✔(第1版: 値の選択・検索・件数。条件/色/トップ10は第2版へ)**[大] オートフィルタ(フィルターアイコン、ドロップダウンから値のチェックボックス選択・数値/テキストフィルター条件・セル色/フォント色フィルター・トップ10・検索フィールド)**(SortData.aspx)
  - 本家: 列見出しのフィルターアイコンから、値の一覧をチェックボックスで選択、数値フィルター(等しい/より大きい/より小さい等)、テキストフィルター(で始まる/を含む等)、セルの色・フォントの色でフィルター、トップ10 抽出、検索フィールドでの絞り込みができる。
  - calc: calc/src/main.rs:135 self.filter は Option<(u32, String)> — 単一列・単一値の一致絞り込みのみ。calc/src/cmds.rs:159-176 "setfilter"/"clear-filter" もその1本の値を入れる/外すだけで、条件式・色基準・複数値選択・検索欄は無い。
- ✔(第2版 2026-08-07: 本家の形の3タブのダイアログに。空白を無視=allowBlank も往復。日付/時間/customは持ち越し=判定せず素通し・板では「このまま保持」)**[大] 許可の種類(整数・小数・日付・時間・テキストの長さ・その他)と比較条件(範囲内/範囲外/等しい/等しくない/より大きい/より小さい/以上/以下)**(DataValidation.aspx)
  - 本家: 「許可」メニューで 任意の値/整数/小数/リスト/日付/時間/テキストの長さ/その他 を選び、リスト以外は「データ」メニューで比較条件を選んで数式・値を指定できる。
  - calc: sheet/src/model.rs:492 コメントに明記「データの入力規則(list だけ)」。Validation 構造体(model.rs:498-501)は range と formula(候補の直書きまたは範囲参照)のみを持ち、整数/小数/日付/時間/テキスト長やその他の比較条件は無い。
- ✔(第2版 2026-08-07: 3タブのダイアログの中に。停止=堰き止め、警告/情報=通して言う)**[大] 入力メッセージタブ・エラーアラートタブ(スタイル:停止/警告/メッセージ、タイトル・本文、表示可否チェックボックス)**(DataValidation.aspx)
  - 本家: データ検証ウィンドウは設定・入力メッセージ・エラーアラートの3タブ構成で、無効なデータ入力時にスタイル別(停止=拒否、警告/メッセージ=確認可)の警告を出せる。
  - calc: calc/src/picks.rs:1264-1308 "validation" 板は formula 文字列を1行受けるだけで、入力メッセージ・エラーアラートに相当する項目・保存フィールドは model.rs の Validation 構造体に存在しない。無効値を入力した際の挙動(拒否/警告)自体も未確認。
- ✔(2026-08-07: 右クリックの並べ替え▸に「選択したセルの色を上に」「選択したフォントの色を上に」— カーソルのセルの色と同じ行を上に集める安定並べ替え。色が無いセルでは項目が灰色)**[中] セルの色・フォントの色を基準にした並べ替え**(SortData.aspx)
  - 本家: 「選択したセルの色を上に」「選択したフォントの色を上に」を右クリックの並べ替えメニューやカスタムソートで選べる。
  - calc: sheet/src/model.rs:1507-1513 の並べ替えキーは cmp_value(セルの値)のみ。色を基準にする分岐は無い。calc/src/picks.rs:386-389 の右クリック「並べ替え」サブメニューも昇順・降順の2項目のみ。
- ✔(2026-08-07: 「重複を削除」で比べる列の入切の板(全列 ☑ が既定)+「先頭行は見出し(消さない)」の入切。選んだ列だけで比べて行を丸ごと消し、何件消したかを言う。部分選択時の範囲拡張の警告は控え=常に表全体)**[中] 重複削除ダイアログの列選択(「すべて選択」既定・個別列のチェック解除)とヘッダー有無チェックボックス**(RemoveDuplicates.aspx)
  - 本家: 「重複を削除」ダイアログで対象列を個別にチェック/解除でき、「データにヘッダーがあります」チェックボックスで先頭行の扱いを切り替えられる。部分選択時は「選択範囲を拡張」/「選択範囲で削除」を選べる警告が出る。
  - calc: calc/src/cmds.rs:1925-1932 "rem-duplicates" は self.book.sheets[self.active].remove_duplicate_rows(true) を無条件に呼ぶのみ(header は常に true 固定)。sheet/src/model.rs:1532 remove_duplicate_rows は常にシート全体・全列を対象に比較する実装で、列の取捨選択・ヘッダー切替・部分範囲の拡張確認ダイアログは存在しない。
- ✔(2026-08-07: outlineLevel 1〜7(ECMA-376 の上限)で入れ子でき、7で頭打ち — 本家の「最大8レベル」は基底+7段の数え方で同じ意味。頭打ちと段ほどきの試験を追加。レベル一括開閉の 1〜8 釦は控え(+/− で足りる))**[小] グループ化の最大レベル数(最大8レベル)**(GroupData.aspx)
  - 本家: 行・列のグループ化は最大8レベルまでネスト可能。
  - calc: calc/src/cmds.rs:1174-1181, 1185-1192 で `(l+1).min(7)` としており、レベルは1から7までの7段階しか作れない(本家の8レベルに対し1段少ない)。

### 表とピボット(11件)

- ✔(第1版 2026-08-07: 比較6種+間/外・文字を含む・重複/一意・上位/下位N・平均上下を判定と xlsx 往復つきで。第2版 2026-08-07: データバー・カラースケール(2/3色)・アイコンセットを追加 — 範囲の最小〜最大の物差し(scalar)で画面に棒/塗り/矢印・●を描き、dataBar/colorScale/iconSet と往復。百分位の区切りは最小/最大の直線で近似。紙(PDF)への描画・日付・空白/エラー・数式・上位%は控え)**[大] ルールタイプ: 上位/下位項目・平均・テキスト・日付・空白/エラー・重複/一意・データバー・カラースケール・アイコンセット・数式**(ConditionalFormatting.aspx)
  - 本家: 10種類以上のルールタイプから選べる(top10/aboveAverage/containsText/timePeriod/containsBlanks等/duplicateValues/dataBar/colorScale/iconSet/expression)
  - calc: xlsx.rs:944,972 でcellIs以外は読み込み時に「報告して落とす」(rep.note)。実装されているのはcellIsのみ。SEKKEI.md 1555行目に明記(「条件付き書式▸(cellIsを持ち・効かせ・往復。他の型は報告して落とす)」)
- ✔(2026-08-07: 合計行の =SUM/=SUBTOTAL のセルで右クリック →「合計の集計のしかた」▸ 8択(合計・平均・個数・最大・最小・標準偏差・分散・なし)。=SUBTOTAL(n, 範囲) に書き換え=絞り込み中の行は数えない。「その他の関数」は式の直打ちで足りるため見送り)**[中] 合計行のセル右のドロップダウンで集計関数を選ぶ(平均・カウント・最大・最小・合計・標準偏差・分散・その他の関数・なし)**(FormattedTables.aspx)
  - 本家: 合計行を追加すると各セル右に▼が出て、SUBTOTAL関数の集計方法(平均/カウント/最大/最小/合計/標準偏差/分散/その他の関数/なし)を選べる
  - calc: calc/src/cmds.rs:1458 の td-total ハンドラは =SUM を固定で入れるのみ(add_total_row)。集計関数を選ぶドロップダウンやSUBTOTAL相当の仕組みは無い
- ✔(第1版 2026-08-08: 見出しの ▼ に「ラベルで絞る…」(含む/で始まる/で終わる — 合わない値を hide に落とす)と「値で絞る…」(> >= < <= = しきい値。集計後の行に掛け、小計・総計も絞り込み後の物差しで出す。joPivot と往復)。日付は「グループ化」で月/四半期/年に束ねてから値の☑☐で絞れる。トップ10・検索欄は控え)**[中] ピボットのフィルター機能(ラベルフィルター・値フィルター・日付フィルター・トップ10オートフィルター・検索フィールド)**(PivotTables.aspx)
  - 本家: フィールドの▼からラベル/値/日付でのフィルター条件を細かく設定できる
  - calc: PivotDef にフィルターフィールドの区分自体が無い(sheet/src/model.rs:633)。calc/src/cmds.rs のピボット関連コマンドにも filter に該当するものは無い
- ✔(第1版 2026-08-08: ▼ の「グループ化…」で 月/四半期/年(日付の文字列を polars で読んで束ねる。読めない値はそのまま残す=黙って落とさない)と「数の幅…」(0起点の帯。札は右詰めで数字順に並ぶ)。「グループ解除」つき。joPivot と往復。開始値/終了値の指定・展開/折りたたみは控え)**[中] ピボットのグループ化(日付・数値、右クリック→グループ、開始値/終了値/単位の指定)**(PivotTables.aspx)
  - 本家: 行/列の項目を右クリックしてグループ化ウィンドウでパラメータ設定、展開/折りたたみアイコン操作
  - calc: calc/src/picks.rs・cmds.rs にピボット項目のグループ化コマンドは見当たらない(pivot-select/pivot-refresh/pivot-totals/pivot-subtotals/pivot-blank/pivot-layoutのみ実装、calc/src/cmds.rs:1297-1370)。データタブの汎用「グループ化」(group/ungroup)はあるがピボット項目専用ではない
- ✔(第1版 2026-08-07: 条件付き書式 →「ルールの管理…」で規則の一覧(範囲+種類の名前)→ クリックで「そこへ移動 / この規則を消す」。優先順位の上下・プレビューは控え — 規則を消して掛け直せば足りる)**[中] 新しい書式設定ルールウィンドウ/ルール管理ウィンドウ(優先順位変更・表示範囲選択・プレビュー)**(ConditionalFormatting.aspx)
  - 本家: モーダルダイアログでルール一覧・編集・削除・優先順位の上下・適用範囲(現在の選択/ワークシート/テーブル/ピボット)を管理できる
  - calc: condformat(calc/src/cmds.rs:1863)は右クリックと同じ一覧(menu_sub="cond")を開くのみで、4項目の固定メニュー(calc/src/picks.rs:400-404)。ルールの一覧管理・優先順位・適用範囲切替のダイアログは無い
- **[小] テンプレート選択ギャラリーからの表スタイル選択**(FormattedTables.aspx)
  - 本家: テーブル作成時に色・模様の異なる複数のテンプレートから選ぶ
  - calc: instable/table-tpl(calc/src/cmds.rs:1491)は固定色(D5E8DC見出し・F1F6F3縞)を一律適用するのみ。テンプレートギャラリーは無い
- **[小] ピボットの並べ替え(昇順/降順、その他の並べ替えオプション)**(PivotTables.aspx)
  - 本家: フィールドの▼から昇順/降順などの並べ替えを行える
  - calc: ピボット関連コマンド一覧(calc/src/cmds.rs:31-33)に並べ替え系は無い。PivotDefにも並べ替え順の保持フィールドが無い
- **[小] スライサー挿入時の列チェックダイアログ(複数列を一度にスライサー化)**(Slicers.aspx)
  - 本家: テーブル設定/挿入タブのスライサーで、対象列をチェックボックスで複数選び、OKで一括作成
  - calc: insslicer(calc/src/cmds.rs:739)はカーソルのある1列を即スライサー化するのみで、列選択ダイアログは無い
- **[小] スライサー設定タブ(幅・高さ・一定の比率・位置・ボタン列数・スタイル選択)**(Slicers.aspx)
  - 本家: スライサーを選択すると右サイドバーに設定タブが出て、サイズ・位置・列数・スタイルを変更できる
  - calc: view.rs:2378以降のスライサー描画は固定幅190px・固定スタイルのポップアップのみ。設定タブ・サイズ変更・スタイル選択は見当たらない
- **[小] 並べ替えオプション(昇順A→Z/降順Z→A)とデータのない項目を非表示にする**(Slicers.aspx)
  - 本家: スライサーの高度な設定で項目の並び順や空データ項目の表示/非表示を切り替えられる
  - calc: スライサーの値リストは BTreeSet で常に昇順固定(view.rs:2403)。降順への切替や空欄項目の非表示オプションは無い(空欄は常に(空白)として末尾に追加、view.rs:2401-2403)
- **[小] キーボードショートカット Alt+S(マルチセレクト)/Alt+C(フィルタークリア)**(Slicers.aspx)
  - 本家: スライサー選択中に Alt+S で複数選択切替、Alt+C でフィルタークリア
  - calc: main.rs内にスライサー向けの Alt+S / Alt+C キー処理は見当たらない。操作はマウスクリックの≡(複数選択切替)と✕(クリア)ボタンのみ(view.rs:2411-2445)

### グラフと画像(5件)

- ✔(第1版 2026-08-07: このアプリで挿した画像・グラフの選択・移動(セル錨+pxずらし、xlsx往復)・比を保つリサイズ・Del 削除。読み込んだ画像は原文持ち越しを守るため動かせない(そう言う)。回転・反転・トリミング・不透明度・置換は控え)**[大] 挿入済み画像の選択・移動・リサイズ・削除・回転・反転・トリミング・不透明度・置換**(InsertImages.aspx)
  - 本家: 画像を選択し8つのハンドルでリサイズ、ドラッグで移動、90度回転・左右上下反転、トリミング(自由形/塗りつぶし/フィット)、不透明度スライダー、ファイル/URL/ストレージからの置換、Deleteで削除、代替テキスト(タイトル・説明)設定ができる
  - calc: 画像は sheet::model::SheetImage として images_new に格納される(calc/src/py.rs:519, calc/src/io.rs:526)が、main.rs 内のマウス処理・shape_at/shape_drag_at(calc/src/main.rs:2157-2200)は shapes_new のみを対象にしており images_new への参照が main.rs に一つも無い(grep images_new calc/src/main.rs が空)。つまり挿入した画像はクリック選択・ドラッグ移動・リサイズ・Delete削除・回転・反転・トリミング・不透明度のいずれも実装が無い。右クリックメニュー(picks.rs)にも画像編集項目は無い。保存(xlsx書き出し、sheet/src/xlsx.rs:1940)は対応済みで見た目は残るが、挿入後に一切動かせない静的な貼り付けになっている
  - 直し方の当たり: shape_at/shape_drag_at の対象に images_new を含めるか、images_new を shapes_new に統合して共通の選択・移動・リサイズ・削除の仕組みに乗せる
- ✔(2026-08-07: グラフは挿した画像(images_new)なので同じ機構で選択・移動・リサイズ・削除)**[大] 挿入されたグラフの選択・移動・リサイズ・削除**(InsertChart.aspx)
  - 本家: グラフをクリックで選択、8つの白いハンドルでリサイズ、ドラッグで移動、Deleteで削除できる
  - calc: グラフは画像としてimages_newに積まれる(前項と同じ経路: py.rsのinsert_chartは最終的にimages_newへpushするsheet::model::SheetImage相当のpush)。images_newはmain.rsのマウス処理対象外のため、グラフ画像も選択・移動・リサイズ・削除ができない
- **[中] スパークラインの種類(カラム型・勝ち/負け型)**(InsertSparklines.aspx)
  - 本家: カラム(棒)型、ライン型、勝ち/負け(正負)型の3種類から選べる
  - calc: inssparkline コマンド(calc/src/cmds.rs:1758-1822)は選択範囲の数値を正規化した折れ線(points: Vec<(f32,f32)>)をkind:"spark"の図形として1種類のみ生成する。種類選択UIは無く、カラム型・勝ち負け型は実装されていない
- **[小] スパークラインの詳細設定(サイドバーのスタイル/表示/軸オプション、空セル扱い、垂直軸スケーリング)**(InsertSparklines.aspx)
  - 本家: 右サイドバーのスパークライン設定タブでテンプレート・色・線の太さ、高点/低点/負の点/最初/最後の点・マーカー表示、詳細設定ウィンドウで空セルの扱い(ギャップ/ゼロ/線で接続)、垂直軸スケーリング(自動/共通/固定)を設定できる
  - calc: inssparkline(calc/src/cmds.rs:1758-1822)は挿入時に固定色(1B6E3C固定)・単純な折れ線のみを生成して完結する。挿入後に選択して設定を変更するサイドバーや詳細設定ダイアログは存在しない(picks.rs, view.rsにスパークライン設定パネルの記述なし)
- ✔(2026-08-07: BMP・GIF も挿せる — 画面の表示・xlsx の media/ContentTypes とも)**[小] 対応画像形式**(InsertImages.aspx)
  - 本家: BMP, GIF, JPEG, JPG, PNG に対応
  - calc: insert_image_dialogのファイルダイアログのフィルタはpng/jpg/jpegのみ(calc/src/io.rs:514)。BMP・GIFは非対応

### 図形とテキスト(10件)

- **[大] 図形設定パネル(塗りつぶし・線・影・回転・不透明度)**(InsertAutoshapes.aspx / InsertSmartArt.aspx / InsertTextObjects.aspx)
  - 本家: 図形/SmartArt/テキストボックスを選ぶと右側に「図形設定」パネルが出て、単色/グラデーション/画像・テクスチャ/パターン/塗りつぶしなしの塗り、線幅0.5〜6pt・線種・不透明度、回転(反時計/時計90度、水平/垂直反転)、影の表示チェックボックスなどを設定できる
  - calc: calc/src/view.rs:1456-1481 の shape_frame は選択枠(点線)と右下のリサイズ用ハンドルの表示のみで、右パネルや塗り・線種・影・回転の操作は無い。SheetShape(sheet/src/model.rs:365-388)には fill/line の単色RRGGBB以外(グラデーション・パターン・線幅・線種・不透明度・回転角)のフィールドが存在しない
- **[大] 図形選択時の右クリックメニュー(配置/整列/回転/マクロ割り当て/画像として保存/ポイント編集/図形の詳細設定/リンク)**(InsertAutoshapes.aspx / ManipulateObjects.aspx)
  - 本家: 図形を右クリックすると 切り取り・コピー・貼り付け・配置・整列・回転・マクロの割り当て・画像として保存・ポイントの編集・図形の詳細設定・リンク の専用メニューが出る
  - calc: calc/src/main.rs:996-1035 の right_click_at は見出し/セルの右クリック処理のみに分岐し、shape_sel(図形選択)時の専用分岐が無い。図形上で右クリックしても通常のセルメニュー(picks.rs の menu_action/menu_sub_entries)が出るのみで図形専用項目は無い
- **[中] 図形ギャラリーの種類の豊富さ**(InsertAutoshapes.aspx)
  - 本家: 「図形」アイコンから、最近使用した図形・基本図形・矢印・数式・チャート(フローチャート)・星とリボン・吹き出し・ボタン・長方形・線 のカテゴリに分かれた多数のオートシェイプを選べる
  - calc: calc/src/picks.rs:26-49 の apply_pick("shape") は 角丸四角形・楕円・右矢印・ひし形・直線・(既定)四角形 の6種類のみで、カテゴリ分けもない。SheetShape.kind のコメント(sheet/src/model.rs:371)も「rect / roundRect / ellipse / rightArrow / diamond / line」の6種と明記
- **[中] 図形の回転(回転ハンドルドラッグ、Shift+ドラッグで15度単位)**(ManipulateObjects.aspx)
  - 本家: 選択した図形の上部に緑色の円形回転ハンドルが出て、ドラッグで自由回転、Shiftを押しながらだと15度刻みに制限できる
  - calc: SheetShape(sheet/src/model.rs:365-388)に回転角のフィールドが無く、main.rs の shape_drag(main.rs:90,726-736)も移動とサイズ変更(corner)の2種類のみ。回転ハンドル自体が存在しない
- **[中] 複数オブジェクトの選択・整列(左/中央/右/上/下揃え)・分布(水平/垂直)・グループ化**(ManipulateObjects.aspx)
  - 本家: Ctrl+クリックで複数図形を選択し、レイアウトタブの配置アイコンから左揃え等の整列、3個以上で分布、2個以上でグループ化ができる
  - calc: shape_sel: Option<usize>(main.rs:88)は単一選択のみを保持する設計で複数選択の概念が無い。ui/src/ribbon.rs:313 で「図形を結合」はx()(灰色/未実装)。SEKKEI.md:1414-1415 に『前面へ移動/背面へ移動/配置/グループ化/図形を結合 が無い(オブジェクト操作は未実装 → 灰色で場所だけ)』と自己申告済み
- **[中] テキストボックス内の段落設定(左/中央/右/上/中央/下揃え、テキスト方向、箇条書き・番号付きリスト、タブ位置、取り消し線・上付き・下付きなどの文字効果)**(InsertTextObjects.aspx)
  - 本家: テキストボックスを選ぶと「段落設定」パネルが出て、垂直方向の配置、テキストの回転方向、リスト形式、タブ位置、フォント効果を設定できる
  - calc: instext(calc/src/cmds.rs:1741-1756)はテキストボックスを「枠の図形+文字」として作り、text: Option<String>(sheet/src/model.rs:381)に生文字列を保持するのみ。段落プロパティ(配置・方向・リスト・タブ・文字効果)の構造がSheetShapeに無い
- **[小] 図形のブール演算(結合・結合(ハイライト)・フラグメント・交差・減算)**(ManipulateObjects.aspx)
  - 本家: 2つ以上の図形を選び、結合・フラグメント・交差・減算などの図形合成ができる
  - calc: ui/src/ribbon.rs:313 x("図形を結合", "shapes-merge") — 灰色で未実装と明示済み(cmds.rs の HANDLED にも "shapes-merge" は無い)
- **[小] 図形の編集(ポイント編集モード、ベジェハンドル、Ctrl+クリックで頂点追加/削除)**(ManipulateObjects.aspx)
  - 本家: 図形を右クリック→「ポイントの編集」で頂点を自由に動かしたり、Ctrlクリックで頂点を追加・削除できる
  - calc: SheetShape に points フィールド(sheet/src/model.rs:382-383)があるが、コメントに「kind="spark"(スパークライン)が使う」とあり折れ線専用。一般図形の頂点編集UIは picks.rs / main.rs に見当たらない
- **[小] SmartArt右クリック「画像として保存」**(InsertSmartArt.aspx)
  - 本家: SmartArtを右クリックして画像としてハードドライブに保存できる
  - calc: SmartArtは「うちの図形(SVG方式)の集まり」として組まれる設計(SEKKEI.md:218-222)。図形専用の右クリックメニュー自体が存在しない(別項目で報告済み)ため、画像として保存の専用操作も無い
- **[小] Shift+ドラッグで比率維持のサイズ変更・水平/垂直限定移動、Ctrl+矢印キーで1px移動**(ManipulateObjects.aspx)
  - 本家: サイズ変更時にShiftで縦横比を保ったまま拡大縮小、移動時にShiftで水平/垂直方向に制限、Ctrl+矢印キーで1ピクセル単位移動ができる
  - calc: shape_drag_at(main.rs:2171)やshape_dragのフィールド定義にshift修飾のハンドリングが見当たらない(grepでshape関連コードにshift分岐なし)。矢印キーでの図形移動処理も見当たらない

### 描画と記号(4件)

- ✔ **[大] Alt+Enter によるセル内手動改行**(Wrap-text-in-a-cell.aspx)
  - 本家: セル編集中に Alt+Enter で任意の位置に改行を挿入できる
  - calc: ui/src/lib.rs の KeyBinding 一覧(179-208行付近)に alt 修飾のバインディングが無く、calc/src/main.rs 全体を grep しても "alt" という語自体が出現しない。Alt+Enter による手動改行のキー割り当ては見当たらない
- **[中] 記号ダイアログ(範囲=カテゴリ別分類、Unicode 16進値入力、特殊文字タブ、最近使用した記号、フォント選択)**(InsertSymbols.aspx)
  - 本家: 「記号」アイコン→「その他の記号」で、範囲(通貨記号など)によるカテゴリ分け・Unicode16進コード直接入力・特殊文字タブ・最近使った記号の履歴・フォント選択ができるダイアログが開く
  - calc: cmds.rs:1539-1554 の "inssymbol" は固定の22文字(〒℡№㈱〆※→←↑↓○●◎△▲×☑☐✓①②③)を並べた一覧を picks.rs の pick_kind="symbol" で表示し、選ぶと picks.rs:20-24 で数式バーへ差し込むだけ。カテゴリ分け・Unicodeコード入力・履歴・フォント切替は無い
- **[中] 行見出し右クリック「行の高さを設定」「行の高さを自動調整」**(Wrap-text-in-a-cell.aspx)
  - 本家: 折り返しでテキストが増えた行の高さを、行見出しの右クリックメニューから数値指定または自動調整で揃えられる
  - calc: picks.rs の menu_sub_entries / menu_action を grep しても "row-height" や「行の高さ」「自動調整」に相当する項目は無い。行の高さ変更は size_drag によるマウスドラッグのみ(main.rs:658-683)。しかもダブルクリックでの自動調整は明示的に撤去されている(main.rs:740-744 のコメント「**ダブルクリックの自動調整は撤去した**(2026-08-03 発注者報告)」)
- **[小] 描画タブの「選択」ボタン・ペン/蛍光ペン/消しゴム**(DrawingOperations.aspx)
  - 本家: ペン・蛍光ペンで自由描画、消しゴムでストローク単位の削除、選択でストロークの選択・サイズ変更・削除
  - calc: cmds.rs:934-951「draw-select」「pen/highlighter/eraser」で実装。消しゴムは main.rs:911-918, 953-961 で ink_at() ヒットしたストロークを丸ごと削除(本家と同じくストローク単位)。選択ボタンは道具を措いてセル操作に戻るのみで、置いたストローク自体のサイズ変更・移動には非対応と見られる(shape_sel は shapes_new の図形用で、ink ストローク専用の選択・リサイズ経路は見当たらない)

### シートと印刷(10件)

- ✔(第1版 2026-08-07: 左/中/右×ヘッダー/フッターの6区分+&P/&N。xlsx の headerFooter と往復し PDF に描く。日付・時刻・画像・書式・奇偶/先頭ページ別・拡縮トグルは控え)**[大] ヘッダー/フッター編集ダイアログ全体(左/中央/右フィールド、ページ番号・ページ数・日付・時刻・ファイル名・シート名・画像の挿入、書式設定、最初のページ別設定、奇数/偶数ページ別設定、拡大縮小・余白合わせトグル)**(InsertHeadersFooters.aspx)
  - 本家: 挿入orレイアウトタブからヘッダー/フッター編集ダイアログを開き、上記の全機能を設定できる
  - calc: ui/src/ribbon.rs:118-119 に c("edit-header",...) / c("edit-footer",...) の定義があるが、レイアウトタブ側(ui/src/ribbon.rs:303)では x("ヘッダー/フッター", "editheader") として灰色・未実装扱い。かつ calc/src/cmds.rs と calc/src/picks.rs を grep しても "edit-header"/"edit-footer"/"editheader" のハンドラが1つも無く、run_cmd に処理が無い(押しても何も起きない)。sheet/src/model.rs にもヘッダー/フッターのテキストや設定を保持するフィールドが皆無で、xlsx の headerFooter 要素の読み書きも未対応
- **[大] 高さ・幅を個別にページ数指定する拡大縮小印刷(fit to N pages wide/tall)**(ScaleToFit.aspx)
  - 本家: レイアウトタブの「縮尺変更」で高さ=1ページ/自動、幅=自動/その他の値を独立して指定でき、縮尺%は自動計算される
  - calc: calc/src/cmds.rs:1634-1648 "scale" は 100→90→80→70→50→100 の単純な%循環トグルのみ。sheet/src/model.rs:330 print_scale: Option<u32> という単一パーセンテージしか持たず、fit-to-width/height に相当するフィールドが無い(grep 0件)。ページ数を指定する拡大縮小は未実装
- **[中] ページ区切りメニュー(挿入/すべてリセット/削除の3択)**(PageBreaks.aspx)
  - 本家: レイアウトタブの区切りアイコン横の矢印を押すと「ページ区切りを挿入」「すべてのページ区切りをリセット」「ページ区切りを削除」の3項目メニューが出る
  - calc: calc/src/cmds.rs:1650-1668 の "pagebreak" はドロップダウンではなく単一釦のトグル。カーソル行に改ページが有れば外し、無ければ追加するだけで、「すべてリセット」に相当する一括解除やメニュー選択肢が無い。ui/src/ribbon.rs:107,302 も c("pagebreak",...) の単発ボタン
- **[中] 縦の改ページ(列の区切り)**(PageBreaks.aspx)
  - 本家: 行だけでなく列方向のページ区切りも挿入できる(点線が縦にも入る)
  - calc: sheet/src/model.rs は row_breaks: Vec<u32> のみを持ち、col_breaks に相当するフィールドが存在しない(grepで0件)。列方向の改ページ機能が無い
- **[中] 挿入した改ページの点線表示**(PageBreaks.aspx)
  - 本家: 挿入したページ区切りが格子上に点線で視覚表示される
  - calc: calc/src/view.rs に row_breaks を参照する描画コードが無い(grep 0件)。改ページ位置の視覚フィードバックが無く、status バーの文言でしか分からない(calc/src/cmds.rs:1664前後)
- **[中] 印刷ページ境界の破線表示(画面上)**(ScaleToFit.aspx)
  - 本家: シート上に印刷ページの境界が破線で表示される
  - calc: calc/src/view.rs に該当描画なし(row_breaks/print_areas を使ったページ境界描画は grep で見つからず)。改ページプレビュー自体が ui/src/ribbon.rs:397 x("改ページ プレビュー", "view-pagebreak") として灰色・未実装
- **[中] 自動保存(2秒ごと/高速25回・秒/厳密10分ごと)**(SavePrintDownload.aspx)
  - 本家: デフォルトで自動保存が動作する
  - calc: calc/src/io.rs 全体を grep しても自動保存タイマーや間隔設定が見つからない。保存は save/save_as の手動実行のみ(calc/src/io.rs:415-416, 630以降)
- **[中] 名前を付けて保存で他形式(XLTX, ODS等)を選べる**(SavePrintDownload.aspx)
  - 本家: 保存時にXLSX以外にXLTX、ODS等の形式を選択できる
  - calc: calc/src/io.rs:418,653 の save_as は rfd::FileDialog に add_filter("Excelブック", &["xlsx"]) のみでフィルタは xlsx 固定。他形式での保存は無い
- **[中] 別形式でダウンロード(PDF, JPG, PNG等)・CSVのエンコーディング/区切り文字選択**(SavePrintDownload.aspx)
  - 本家: ファイル>ダウンロードで複数形式を選べ、CSV選択時はエンコーディングと区切り文字を設定できる
  - calc: calc/src/py.rs 内にCSVは読み込み(インポート)専用の CSV_PY スクリプトのみ存在(calc/src/py.rs:216-236, 1148以降)。CSV/TSVへのエクスポートやエンコーディング・区切り文字選択の機能はコード全体で見当たらない。PDF書き出しは io.rs にあるが、これは印刷機能の実体であり「ダウンロード」メニューとしての多形式選択UIではない
- **[中] 複数の固定印刷範囲(各範囲を別ページで印刷)**(SavePrintDownload.aspx)
  - 本家: 印刷範囲を複数設定でき、それぞれ別ページとして印刷される
  - calc: sheet/src/model.rs:328 print_areas: Vec<(Pos,Pos)> は複数保持できる型だが、calc/src/cmds.rs:229 の printarea コマンドは常に vec![range] で1件に上書きし追加ができない。さらに calc/src/io.rs:589-598 で areas.first() のみを使い、2件目以降は「残り{}域の印刷範囲はまだ出せません」という文言で明示的に未対応と表示される(io.rs:598)

### 保護(5件)

- **[中] 許可する操作のチェックボックス一覧(ロックされたセルの選択・書式設定・列/行の挿入削除・並べ替え・オートフィルター・ピボット・オブジェクト編集・シナリオ編集など)**(ProtectSheet.aspx)
  - 本家: シート保護ウィンドウに、保護中でも許可する操作を個別に選べるチェックボックスリストがある
  - calc: sheet::model の Sheet には protected: bool(sheet/src/model.rs:307)のみで、許可操作の粒度を持つフィールドが無い。calc/src/cmds.rs:52 PROTECTED_OK は「保護中でも通す calc 側コマンド」の固定表であり、保護ごとに利用者が選べる設定ではない
- **[中] セル単位のロック/アンロック(既定はロック、書式設定の保護タブでチェックを外すと保護中も編集可)**(ProtectSheet.aspx)
  - 本家: セルの書式設定に「ロック」チェックボックスがあり、個々のセルをロック解除して保護対象から外せる
  - calc: sheet::model 内にセル単位の locked 属性は見当たらない(grep で `locked` はロックファイル(排他ロック)の意味のみ、main.rs:83-86)。保護はシート全体の一括 on/off
- **[小] シートタブの右クリックメニューからの保護/保護解除**(ProtectSheet.aspx)
  - 本家: シートタブを右クリックすると「シートの保護」「保護の解除」のメニュー項目が出る(保護開始の3経路の一つ)
  - calc: calc/src/picks.rs:1456-1469 open_sheet_menu のメニュー項目は [挿入,削除,名前の変更,コピーを作成,左へ移動,右へ移動,非表示,再表示,タブの色] のみで、保護/保護解除の項目が無い。保護タブのリボン釦(prot-doc)からのみ操作可能
- **[小] 保護タブの暗号化ボタン・ファイルタブからの保護オプション、パスワード設定・確認、パスワード表示/非表示アイコン**(Password.aspx)
  - 本家: 暗号化をリボンの保護タブ・ファイルタブの双方から設定でき、パスワード文字の表示/非表示を切り替えるアイコンがある
  - calc: calc/src/cmds.rs:470-475 prot-encrypt はリボンの保護タブからのみ(ファイルタブ相当の経路は確認できず)。パスワード入力は view.rs:1844-1845 で常時マスク(●)表示のみで、表示/非表示切替アイコンは無い
- **[小] 図形ロック・テキストロック・数式を隠す(数式バー非表示化)などシート保護の詳細項目**(ProtectSpreadsheet.aspx)
  - 本家: 図形の選択時ロック、テキストのロック、数式を隠すチェックボックスがある
  - calc: 対応する項目は calc/src/cmds.rs、sheet/src/model.rs のいずれにも見当たらない。保護はシート全体のbool のみ

### 関数A(6件)

- ✔ **[大] F9(再計算)・Shift+F9(現在のシートのみ再計算)キー**(InsertFunction.aspx / GoalSeek.aspx)
  - 本家: 数式タブの「計算」から F9 で全体、Shift+F9 でアクティブシートのみ再計算。手動計算モード時はこれで明示的に計算を走らせる
  - calc: calc/src/cmds.rs:882 の "calc-mode" はモードの自動/手動トグルのみで、手動時に計算を実行する手段(F9キーもリボン釦も)が実装に存在しない。calc/src/main.rs:1560 の recalc_if_auto は auto_calc=false の間は常に何もしない。ステータス文言は「F9 で計算します」と案内する(cmds.rs:886)が、そのキーはどこにもバインドされていない(main.rs の a_* 一覧、repo全体を "f9"/"shift-f9" で grep して該当なし)
- ✔(2026-08-07: 「名前の管理」が一覧に — 名前(とテーブル名)を並べ、選んで 移動/中身の打ち直し/削除。新規はいまの選択に付ける。本家のフィルタは件数が要る規模になったら)**[大] 名前マネージャー(一覧・フィルタ・編集・削除・新規)**(UseNamedRanges.aspx)
  - 本家: 名前マネージャーウィンドウで既存の名前付き範囲を一覧表示し、「すべて」「定義された名前」「テーブル名」等でフィルタ、「編集」「削除」「新規」ボタンで管理できる
  - calc: calc/src/cmds.rs:1871-1874 の "defname" は常に self.prompt = Some(("name", Editor::new(""))) という単一テキスト欄の板を開くだけ(picks.rs:757-782のfinish_prompt "name"アーム)。既存の名前の一覧表示・編集・削除の手段はコード上どこにも無い(名前を消すには同名で別範囲を再定義して上書きするしかない: picks.rs:777 s.names.retain)
- **[中] 名前の貼り付け(名前をリストから選んで数式へ挿入)ダイアログ**(UseNamedRanges.aspx)
  - 本家: 「名前の貼り付け」メニューから既存の名前をリストで選んで数式に挿入できる
  - calc: repo全体を "名前の貼り付け"・"paste.*name" で grep しても該当なし。名前をたぐる手段は数式直入力時のオートコンプリート(picks.rs:499以降)のみ
- **[中] 名前の適用範囲(ブック全体 or 特定シート)を選べる「新しい名前」ダイアログ**(UseNamedRanges.aspx)
  - 本家: 名前作成時にスコープ(ワークブック全体/特定のワークシート)を選べる
  - calc: sheet/src/model.rs のシート構造体は names をシートごとに保持(grep: "s.names"/".names.push" は全てsheet単位)。ブック全体スコープの名前という概念自体が実装に無く、スコープ選択UIも無い(常にアクティブシートに定義)
- **[中] ウォッチの個別削除(Ctrl+左クリックで選択→「ウォッチを削除」、または「すべて削除」)・ダブルクリックでセルへ移動**(WatchWindow.aspx)
  - 本家: ウォッチ項目を個別に選んで削除できる。ダブルクリックで該当セルへジャンプできる
  - calc: calc/src/cmds.rs:901-925 の "watch" は選択範囲の式セルを追加するか(既存分は無視)、追加が無ければ self.watch.clear() で全消去するのみ(cmds.rs:917-920)。個別削除やダブルクリックでのジャンプは watch_bar (view.rs:1246-1264) にクリックハンドラが無く実装されていない
- **[小] Shift+F3 で関数の挿入を開くキーボードショートカット**(InsertFunction.aspx)
  - 本家: Shift+F3 キーで「関数の挿入」ダイアログを開ける(ホームタブ釦・数式タブ・右クリック・fxアイコンと並ぶ複数経路の一つ)
  - calc: calc/src/main.rs の a_* アクション一覧、および repo 全体を "f3"/"shift-f3" で grep しても該当なし。insert-function はリボン釦(ui/src/ribbon.rs:317)と cmds.rs:842 の実装のみで、キー割り当ては無い

### 関数B(2件)

- **[中] レガシー配列数式(範囲選択→数式入力→Ctrl+Shift+Enterで確定、セル範囲全体に同じ式が入り数式バーに{}表示される旧来型CSE配列数式)**(InsertArrayFormulas.aspx)
  - 本家: セル範囲を選択し数式バーに数式を入力後、Ctrl+Shift+Enterを押すと選択範囲全体に配列数式として入り、数式が{}で囲まれる。編集時も再度Ctrl+Shift+Enterで確定。全消去時はセル全選択でDelete。
  - calc: calc/src/main.rs:1976 a_enter は quit_ask/name_edit/fn_args/fn_dlg/solver/prompt/shape_sel の分岐のみで、Shift+EnterやCtrl+Shift+Enterの特別処理は無い。Enterキーは常に move_cursor(1,0) に落ちる。sheet/src/calc.rs:2479 ARRAY_FNS は FILTER/SORT/UNIQUE/SEQUENCE/TRANSPOSE の5つの動的配列(スピル)関数のみを「配列数式」として自動判定する独自方式(sheet/src/calc.rs:2777 is_array_formula)であり、本家のCSE型配列数式とは仕組みが異なる。
- **[小] パーセント書式のショートカットキー Ctrl+Shift+%**(Calculate-percentages.aspx)
  - 本家: セルを選択しCtrl+Shift+%を押すとパーセント書式が適用される。
  - calc: calc/src/main.rs 内に '%' や percent に関連するキー割当が見つからない(grep該当なし)。リボン釦(cmds.rs:1935)と右クリック(picks.rs:409)からは可能だが、キーボードショートカットの直接割当は無い。

### ツールと設定(9件)

- ✔(第1版 2026-08-07: ネイティブファーストで意味のある項目に絞る — 言語・テーマ(明暗、設定に永続)・画面の文字の大きさ(−/+)・書体・校正の宛先・Python・名乗り。本家の残り(自動保存・共同編集モード・R1C1・地域・反復計算・マクロ3択など)はサーバー前提か別項目の控え)**[大] 詳細設定パネル全体(編集と保存/共同作業/外観/ワークスペース/地域設定/校正/計算の7区分・30項目超)**(AdvancedSettings.aspx)
  - 本家: 自動保存、貼り付けオプション表示、関数ツールチップ、共同編集モード(高速/厳格)、他ユーザーの変更表示、コメント表示、インターフェーステーマ(8種)、ドキュメントダークモード、タブスタイル、スクリーンリーダー支援、R1C1参照スタイル、Altキーナビ、クイックプリント、スナップ、スクロールバー表示、シート方向、クイックアクセスカスタマイズ、測定単位、デフォルトズーム、フォントヒンティング、キャッシュモード、マクロ設定(3択)、キーボードショートカット編集、数式の言語(33言語)、地域(34地域)、区切り記号設定、校正辞書設定、大文字/数字を含む単語の無視、1904年日付システム、反復計算(有効/最大反復回数/最大変更量)
  - calc: calc/src/view.rs:2222-2261 の「詳細設定」パネルは、言語(リボン文言)、書体(OFFICE_FONT環境変数の表示のみ)、校正の宛先、Pythonの経路、名前(ロック識別)の5項目のみ。本家の詳細設定の大半の項目(自動保存トグル、共同編集モード、テーマ、R1C1、測定単位、ズーム既定値、マクロ設定、地域設定、反復計算、1904年日付等)に対応する画面が無い。なお個々の機能はリボン側に分散配置されている(theme=ui/src/ribbon.rs:400,calc/src/cmds.rs:1026、zoom=cmds.rs:1087-1093、gridlines/formula-bar/headings=cmds.rs:1094-1109)ため、パネルとしての一元窓が無いだけで機能自体は一部存在する
- **[中] TXT/CSVインポートウィザード(区切り文字・エンコーディング選択、プレビュー、配置範囲指定)**(importdata.aspx)
  - 本家: データタブ→データを取得→4セクション構成のテキストインポートウィザード。エンコーディング(既定UTF-8)、区切り文字(カンマ/セミコロン/コロン/タブ/スペース/その他)、詳細設定(小数点区切り・千単位区切り・テキスト修飾子)、プレビュー、配置セル指定、OK
  - calc: calc/src/py.rs:1150 import_text_dialog は rfd のファイル選択ダイアログのみで、区切り文字・エンコーディング・プレビュー・配置範囲などの設定画面は無い。ファイルを選ぶと即座にPython側(CSV_PY)で自動判定して現在のカーソル位置から値として流し込む(calc/src/py.rs:1178-1193)。ウェブアドレス/XMLからのインポートも無い
- **[中] R1C1参照スタイル**(AdvancedSettings.aspx)
  - 本家: セル参照をA1形式からR1C1形式に切り替えるオプション(デフォルト無効)
  - calc: grep でR1C1はfuncs.rsのADDRESS/INDIRECT関数説明文にのみ出現。実際の参照表示切替やパースの実装は見当たらない
- **[中] 反復計算(循環参照の反復解決)・1904年日付システム**(AdvancedSettings.aspx)
  - 本家: 反復計算を有効にする(最大反復回数・最大変更量を指定)、1904年日付システムを使用するオプション
  - calc: sheet/src/calc.rs には財務関数(IRR/RATEなど)個別の反復解法はあるが、循環参照全般に対する「反復計算モード」の設定は見当たらない。1904年日付システムの切替も見当たらず(DATE関数の説明文言に「ブックの日付システムに応じて」とあるが固定1900系と見られる)
- **[小] 数学オートコレクト(記号置換)・認識される関数・入力中の自動フォーマット**(MathAutoCorrect.aspx)
  - 本家: ファイル→詳細設定→スペルチェック→校正→オートコレクトオプションに3タブ(数学オートコレクト/認識される関数/入力中に自動フォーマット)の設定画面
  - calc: grep で cmds.rs・picks.rs に該当箇所なし。詳細設定パネル(calc/src/view.rs:2222-2261)にも校正・オートコレクトの節が無い
- **[小] 所有者・アップロード日・最終更新日の表示**(ViewDocInfo.aspx)
  - 本家: ファイル→情報に「所有者」「アップロード日」「最終更新日」の各フィールドを表示
  - calc: calc/src/view.rs:2318-2333 の「ブックの情報」パネルには統計(シート/使っているセル/式のセル/図形と画像)とプロパティ(作成者/タイトル/タグ/件名/コメント)のみで、所有者・アップロード日・最終更新日の表示は無い(ローカルファイル前提のため文書クラウド側メタデータが無い設計と見られる)
- **[小] プロパティを追加(カスタムフィールド:タイトル・タイプ・値)**(ViewDocInfo.aspx)
  - 本家: 「プロパティを追加」ボタンでタイトル・タイプ・値を入力しOKで確定するカスタムプロパティを作成できる
  - calc: calc/src/view.rs:2338-2367 のプロパティ節は作成者/タイトル/タグ/件名/コメントの固定5項目のみで、カスタムプロパティの追加機能は見当たらない
- **[小] 著者フィールドの複数入力(Enterで追加、×で削除)**(ViewDocInfo.aspx)
  - 本家: 著者は複数人を追加でき、Enterキーで追加、×ボタンで削除できる
  - calc: calc/src/view.rs:2340 の「作成者」は単一のテキスト欄(prop-creator一つ)で複数著者の追加/削除UIは無い(sheet/src/model.rs:658 BookProps.creator はString型)
- **[小] ズーム操作(右下隅の+/-ボタン、パーセント表示クリックで選択肢、50〜500%)**(Navigation.aspx)
  - 本家: ステータスバー右下でズームイン/アウトボタンとパーセント表示があり、クリックで選択肢一覧が出る
  - calc: calc/src/cmds.rs:1087-1093 で zoom-in/zoom-out は実装(0.1刻み、0.5〜2.0すなわち50%〜200%の範囲)。本家は50〜500%だが、calc の上限は self.zoom.min(2.0) すなわち200%までで500%に届かない。またパーセント表示クリックでの選択肢一覧の有無は未確認

### コラボ(2件)

- **[中] シートビュー(表示プリセット)機能そのもの**(SheetView.aspx)
  - 本家: 表示タブ→シートビュー→ビュー マネージャーで「新規」プリセット作成(自動命名View1/2/3…)、「ビューに移動」で有効化・切替、「閉じる」で終了。マネージャーで名前変更・複製・削除ができる。共同編集時に各利用者が自分のフィルタ/表示状態を他人に影響させずに保持できるのが主目的。
  - calc: ribbon.rs に該当の釦が無い(grep で「シートビュー」「view manager」ヒットなし)。表示タブ(ui/src/ribbon.rs:180-198, 394-410)には「シートの表示」という名の釦(id=sheet-view)があるが、これは全く別機能でシート自体の表示/非表示切替(calc/src/cmds.rs:861-888)。ビュー・プリセットの保存/切替/複製/削除の仕組みはコード上に存在しない。
- **[中] コメントの返信(スレッド)・解決(resolve)・並べ替え(日付/著者/グループ/ステータス)・削除範囲選択(現在/自分/すべて)**(Commenting.aspx)
  - 本家: コメントパネルで「返信を追加」によるスレッド化、解決ドロップダウンでステータス管理(オープン/解決済み)、並べ替えオプション、削除時に現在のコメント・自分のコメント・すべてを選べる。
  - calc: sheet::model の comments は `BTreeMap<Pos, String>`(sheet/src/model.rs:314)— セル1つにつき文字列1本のみ。返信・スレッド・解決状態・作成者・日時のフィールドが無い。calc/src/picks.rs:783-793 の finish_prompt("comment") もテキストを丸ごと上書きするのみ。並べ替え・解決・複数コメント管理のコードは存在しない。

### ショートカットと形式(6件)

- **[中] 書式ショートカット(太字 Ctrl+B・斜体 Ctrl+I・下線 Ctrl+U・取り消し線 Ctrl+5)**(KeyboardShortcuts.aspx)
  - 本家: セル選択中に Ctrl+B/I/U/5 で太字・斜体・下線・取り消し線を切り替えられる
  - calc: リボンの太字/斜体/下線/取り消し線は cmds.rs:89-92 ("bold"/"italic"/"underline"/"strikeout") と ribbon.rs:77-80 で実装済みだが、キーボードからの割り当ては ui/src/lib.rs:179-224 の bindings() に存在しない(Ctrl+B/I/U/5 とも未登録)。ボタンでしか切り替えられない。
- **[中] 再計算 F9(全体)/Shift+F9(シート)、計算方法(手動時)のショートカット**(KeyboardShortcuts.aspx)
  - 本家: F9 で全体再計算、Shift+F9 でアクティブシートのみ再計算
  - calc: 手動計算モードと再計算自体は main.rs:180, cmds.rs:145,721,1478,1921,1930 等に実装されているが、F9/Shift+F9 のキー割り当ては ui/src/lib.rs の bindings() に存在しない。cmds.rs:837 のコメントも「手動のときは F9 で計算します」と案内しているだけで、実際のキー結線は見当たらない。
- **[中] 検索 Ctrl+F / 検索と置換 Ctrl+H**(KeyboardShortcuts.aspx)
  - 本家: Ctrl+F で検索、Ctrl+H で検索と置換のダイアログが開く
  - calc: ui/src/lib.rs:194-195 で ctrl-f・ctrl-h とも同一の Find アクションに割り当てられているが、Find アクションのハンドラは writer/src/main.rs にのみ存在し(grep で確認)、calc 側に `ui::Find` を処理するメソッドが無い。calc の検索・置換はリボンの "replace" (cmds.rs:1720-1725, picks.rs:1218-1259 の板2枚方式)でのみ到達でき、Ctrl+F/Ctrl+H キーは calc では機能しないとみられる。
- **[小] Alt キーヒント(Alt でタブ/要素にヒント表示)、Alt+F(ファイルパネル)、Alt+Q(チャット)、Alt+PageUp/Down(シート移動)、F4(参照タイプ切替)などの Alt/F 系ショートカット全般**(KeyboardShortcuts.aspx)
  - 本家: Alt キーでキーヒントが出て各要素にアクセスでき、F4 でセル参照の絶対/相対切替、Alt+PageUp/Down でシート切替ができる
  - calc: ui/src/lib.rs の bindings() には alt- 系のキー割り当てが一切なく、grep でも "alt-"/"Alt+" の実装が main.rs に見当たらない(コメント中の「Alt+F4」への言及のみ)。F4 による参照タイプ切替も未実装。
- **[小] 印刷 Ctrl+P、フルスクリーン F11、別名でダウンロード Ctrl+Shift+S、ズームリセット Ctrl+0、ヘルプ F1、日付/時刻挿入 Ctrl+;/Ctrl+Shift+;、数値形式ショートカット群(Ctrl+1 書式ダイアログ、Ctrl+Shift+$ 通貨、Ctrl+Shift+% パーセント等)、フィルター有効化 Ctrl+Shift+L**(KeyboardShortcuts.aspx)
  - 本家: 各機能に対応するキーボードショートカットが動作する(印刷・フルスクリーン・別名保存・ズーム・ヘルプ・日付時刻挿入・数値書式・フィルター)
  - calc: 対応する機能自体はリボン/右クリックにほぼ実装済み(印刷=PDF出力 io.rs:490, cmds.rs:155、フィルター cmds.rs "setfilter" ribbon.rs:254、数値形式 cmds.rs:1934-1935、書式パネル cmds.rs:296 "format"/"cell-format" 等)だが、ui/src/lib.rs の bindings() にはこれらに対応するキー登録が一件も無い。日付/時刻の直接挿入コマンド自体も grep で見当たらず未実装の可能性がある。
- **[小] 画像形式(JPG/JPEG, PNG, HEIF)の閲覧専用取り込み**(SupportedFormats.aspx)
  - 本家: 本家はこれらの画像ファイルを閲覧目的で開ける(編集不可)と明記
  - calc: calc はブック(xlsx)としてこれらを開く機能ではなく、シート内に画像を挿入する機能(cmds.rs "insimage", main.rs insert_image_dialog)のみを持つ。本家の「画像ファイルをスプレッドシートエディターで直接開いて閲覧する」機能に対応する実装は見当たらない。

## 検証で落ちた・方針扱いの件(誤検出の記録と再発防止)

### 基本操作
- 外部リンク管理ウィンドウ(データタブ「外部リンク」) — 独自の割り切り
- 外部参照式(=[Book1.xlsx]Sheet1!A1)のクリック作成 — 独自の割り切り(他ブックは値として取り込む設計。リンク切れの帳票を作らない)
- 共同編集の「高速モード」使用時、最後の操作の元に戻す/やり直しができない制限 — 独自の割り切り
- エディターヘッダーのスプレッドシート名をクリックしてその場で名前変更(Enterで確定) — 確認できず

### 文字の書式
- 上付き文字(superscript) — 訂正。ribbon.rs:81,233 に c("superscript",...) の登録があり cmds.rs の run_cmd/HANDLED にハンドラが無いという食い違い自体は事実(cmds.rs:796 のコメント「上付きは本家 calc にも無い」、797-799 で subscript のみ実装)。ただし SEKKEI.md 1455行に『控: …上付き(モデルに subscript しか無い)』と明記されており、上付き非搭載は意図的な後回し(方針)である。calc_state の記述(実装が無い)は正しいが、位置づけとしては単なる『食い違い』ではなく SEKKEI 済みの『独自の割り切り』(ボタン登録の掃除漏れ込みで既知の控え)に訂正すべき。
- フォントサイズの範囲とカスタム入力 — 確認できず
- リンクの削除(右クリック「リンクの削除」/Deleteキー) — 確認できず

### セルのプロパティ
- 結合解除(セルの結合解除) — 独自の割り切り
- 均等割付(distributed) — 確認できず
- インデント調整(左揃え時の字下げ、文字数指定) — 訂正。decoffset/incoffset という id は ui/src/ribbon.rs:89-90 に存在するが、これは WRITER タブ(61-213行)専用で、CALC タブ(214行以降、ホームは220-270行)には同id・同機能が定義されていない。calc 側では意図的に未搭載であり「見落とし」ではなく「writer にはあるが calc には積んでいない」独自の割り切りに近い(ただし SEKKEI.md に明記した方針記述は見当たらず、単なる未実装の可能性もある)。数値としては calc に該当機能・フィールドが無いことは確認どおりで、判定「無い」自体は妥当。
- 縮小して全体を表示(shrink to fit)チェックボックス — 訂正。sheet/src/model.rs:205-206 に pub shrink: bool フィールドが実在し、styles.rs:197,524 で xlsx の shrinkToFit を読み書きし、view.rs:689,1051-1053 で描画にも反映している。ただし calc の UI(fmt_panel や cmds.rs)から shrink を ON/OFF するボタン・チェックボックスは無い(fmt_panel の中身は view.rs:2543-2587 で確認済み、shrink 関連の btn/toggle 無し)。つまり『xlsxで既にshrinkが設定されたファイルは正しく開いて表示するが、calc単体でユーザーがこのチェックボックスを操作する経路が無い』が実態。元判定は「grepで該当なし」としたが実際は model/io/描画に実装があり、UI操作面のみ欠けている——「無い」で結論は合っているが根拠説明を訂正する。
- テキスト方向(左から右/右から左)の切替 — 確認できず
- 会計スタイル・パーセントスタイル・カンマスタイル・小数点の増減アイコン — 独自の割り切り

### セルと行列の管理
- Ctrl+Shift+- によるセル削除のキーボードショートカット — 確認できず
- 「テキストを列に分割」の区切り記号選択(カンマ・セミコロン・コロン・タブ・スペースのプリセット+その他カスタム欄) — 独自の割り切り

### データ処理
- 削除完了後の結果ウィンドウ(削除数・残存ユニーク値数の表示) — 独自の割り切り
- セル内ドロップダウンリスト表示チェックボックス、「同じ設定を持つ他のセルにもこれらの変更を適用する」チェックボックス — 独自の割り切り
- 行・列のグループ化とグループ解除、詳細の折りたたみ/展開、アウトラインをクリア — 独自の割り切り
- 「詳細の下に要約行」「詳細の右に要約列」チェックボックス(集計行・列の位置指定) — 確認できず

### 表とピボット
- 表の右クリックメニュー/テーブルデザインタブから「スライサーを挿入」「ピボットを作成」 — 独自の割り切り
- フィールドの選択(フィルター・列・行・値の4セクションへドラッグ)を持つサイドバー式ピボット設定 — 独自の割り切り
- 配置場所の指定(新しいワークシート/既存のワークシート) — 確認できず
- スライサーの絞り込みはテーブル・ピボットの表示行に実際に反映(フィルタ相当の挙動) — 独自の割り切り
- ルールタイプ: 値が(セル比較) — 独自の割り切り

### グラフと画像
- グラフの種類(縦棒以外)・挿入後の種類変更・データの選択・レイアウト/軸/3D回転などの詳細設定 — 独自の割り切り
- スパークラインは元データの変更に追随して自動更新される — 独自の割り切り
- スパークラインの削除(右クリック→「選択されたスパークライン/グループをクリア」) — 独自の割り切り
- チャートへのマクロ割り当て(右クリック→マクロの割り当て) — 独自の割り切り
- 画像の挿入元(ファイルから/URLから/ストレージから) — 独自の割り切り

### 図形とテキスト
- テキストアート(WordArt)挿入 — 確認できず
- SmartArtの挿入(分類→レイアウトの2段選択) — 独自の割り切り

### 描画と記号
- 描画タブのペン・蛍光ペンの色/太さカスタマイズ — 訂正。色・太さのカスタマイズ機能は実装されていない(観察は正しい)が、SEKKEI.md:509-524 に『ペンは細く濃く/蛍光ペンは太く薄く』という固定仕様が明記されており、これは実装漏れではなく方針による意図的な割り切り。status は「無い」ではなく「独自の割り切り」とすべき
- 記号の挿入先 — 独自の割り切り
- 列幅変更時の折り返しテキストの自動追従 — 確認できず
- 方程式エディタ全体(プレースホルダー式のビジュアル編集、分数/スクリプト/根号などのテンプレートから組み立て、右クリックでの引数・行列の追加削除、括弧サイズ制御、Unicode/LaTeX切替、インライン表示、ツールバー表示切替) — 独自の割り切り
- 方程式の挿入先 — 独自の割り切り

### シートと印刷
- 印刷対象(全シート/事前選択セル範囲)の切り替えUI — 独自の割り切り

### 保護
- シート保護のパスワード設定・パスワード確認 — 独自の割り切り
- 編集範囲を許可する機能(新しい範囲ウィンドウ・タイトル・範囲指定・パスワード) — 訂正。ui/src/ribbon.rs:391 `x("範囲を保護する", "protect-range")` で灰色(未実装)。ただし SEKKEI.md:1422-1424 に『本家 SSE は暗号化/ブックの保護/シートの保護/範囲の保護。…ブック/範囲は灰色で場所だけ』と明記されており、単なる『無い』ではなく設計として意図的に見送った独自の割り切り。
- 4つの保護オプション(暗号化/ワークブックを保護/シートを保護/範囲を保護)の一覧構成 — 独自の割り切り
- ブックの保護(シートの表示・追加・移動・削除・非表示・名前変更を禁止、パスワードあり/なし) — 訂正。ui/src/ribbon.rs:389 `x("ブックを保護する", "protect-workbook")` で灰色(未実装)であることは元判定通りだが、SEKKEI.md:1422-1424 に『本家 SSE は暗号化/ブックの保護/シートの保護/範囲の保護。…ブック/範囲は灰色で場所だけ』と明記されており、これは調査漏れではなく意図的に見送った独自の割り切り(設計判断として文書化済み)。
- 範囲の保護(保護された範囲ウィンドウ・新しい範囲・タイトル・範囲選択・編集できるユーザー指定・フィルター) — 訂正。ui/src/ribbon.rs:391 で x() = 灰色(未実装)である点、sheet::model に範囲保護のデータ構造がない点は元判定通り正しいが、SEKKEI.md:1422-1424 に『範囲は灰色で場所だけ』と明記された既定路線であり、単なる欠落ではなく独自の割り切りとして文書化済みの方針。

### 関数A
- ステータスバーの計算表示(平均・個数・最小・最大・合計)を右クリックでカスタマイズ — 訂正。calc/src/io.rs:316-346 sel_stats() は合計/平均/個数の3種固定。SEKKEI.md:718-723 に『下端のステータスバーは...選択の生きた値(合計・平均・個数)を出す — Excel の下端と同じ、writer の文字数に当たるもの』と明記されており、意図的にこの3種のみを実装した設計判断。カスタマイズ機能自体を作らない旨は明言されていないが、3種に絞った経緯が設計文書に残っており「見落とし」ではなく独自の割り切りに分類するのが妥当。
- ウォッチウィンドウのダイアログ形式(ブック・シート・名前・セル・値・数式の列を持つ一覧パネル) — 独自の割り切り
- ゴールシークのステータスウィンドウで結果確認後、再度OKを押して値を置き換える2段階の確認フロー — 独自の割り切り
- ゴールシークダイアログの3フィールド(設定するセル/目標値/変更するセル)と範囲選択アイコン — 独自の割り切り
- 解法の選択肢としてのドロップダウン(シンプレックスLP等、複数手法から選べる) — 独自の割り切り
- ソルバーの結果ウィンドウ(「解を保持」「元の値を復元」「パラメーターダイアログに戻る」の選択) — 独自の割り切り

### 関数B
- 配列関数リスト(CELL等、CSE入力を要する古典的配列関数群)の一部 — 確認できず
- IF関数によるシート間参照と文字列切替のテンプレート技法(=IF(Integration!C3=1,"...","...") 形式) — 独自の割り切り

### ツールと設定
- 統計セクション(ページ数・段落数・単語数・記号数・スペース込み記号数) — 独自の割り切り
- マクロ設定(すべて無効にする/通知を表示/すべて有効にする) — 独自の割り切り
- インターフェーステーマ(8種類のプリセットから選択) — 訂正。SEKKEI.md:649『インターフェイステーマ(画面の明暗)は別物 — 額縁だけ暗くし、セルは白のまま(writer の「紙は白のまま」と同じ、画面と紙の一致)』と明記されており、8種プリセットではなく明暗2値トグルであることは意図的な設計判断(独自の割り切り)。実装は calc/src/cmds.rs:1026-1028 `"theme" => { self.dark = !self.dark; ... }` で確認。なお配色(テーマ色の組。Office/暖色/寒色/墨の4組)は別途 "colorschemas"(cmds.rs付近)で実装されており、本家の「配色」に相当する機能は存在する。ステータスを『食い違い』から『独自の割り切り』に訂正すべき

### コラボ
- メンション機能(「@」「+」で相手を指定・通知) — 独自の割り切り
- バージョン履歴(一覧・差分ハイライト表示・復元) — 独自の割り切り
- 高速モード/厳密モードの切替、リアルタイム同時編集、他ユーザーの選択範囲・カーソル位置のハイライト表示 — 独自の割り切り
- ライブビューアーモード(閲覧専用ユーザー向けリアルタイム閲覧) — 独自の割り切り
- チャットパネル(リアルタイムメッセージ、セッション内のみ保持、未読アイコン変化) — 独自の割り切り

### ショートカットと形式
- xlsx 以外の対応形式(CSV/TSV/ODS/OTS/XLS/XLSB/XLTX/XLTM/XML/ET/ETT/FODS/SXC 等)の読み込み・保存 — 独自の割り切り
- 値のみ貼り付けのキー割り当て — 確認できず


## 挙動の点検(2026-08-07 — 「有るだけで挙動が本家と違う」の観点で済み分を一巡り)

ピボット・表のデザインの常設タブ(→文脈タブに修正済み 0f3106d)と同種の見落としを、
vendor の実物コードと突き合わせて洗った。根拠の行番号つき。

- ✔(2026-08-07 修正)**[高] 組み込み書式ID(numFmtId)の欠落** — sheet/src/styles.rs
  builtin() が 1-4/9/10/14/20/22 のみで、11-13(指数・分数)・15-19/21(日付時刻)・
  37-40(負数の括弧・赤)・45-48(経過時間)・**49(@=文字列)** が無かった。
  実物 xlsx でこれらの既定書式が「一般」に落ち、保存し直すと書式そのものが消えていた。
  本家 sdkjs の aStandartNumFormats と同じ表に揃えて修正(5-8・23-36・41-44 は
  本家も未定義=ロケール依存・予約なので None のまま)。
- ✔(2026-08-07 修正: run_cmd の門番+釦の灰色)**[中] ピボットの上での操作の締め** — 本家 controller/Toolbar.js:3661 はピボット上で
  結合・オートフィルタ・データ検証・重複削除・テーブル挿入を灰色にする。うちは
  締めておらず、ピボット出力の上で結合などができて壊せる。
- ✔(2026-08-07 修正: 3択の確認+sort_range 新設。並べ替え後の sync_input 漏れも同時に修正)**[中] 並べ替えの「選択範囲の拡張」確認** — 本家は選択が表の一部のとき
  「拡張して並べ替え/選択だけ」を聞く(ja.json:1764 txtExpandSort)。うち
  (calc/src/main.rs sort_active)はカーソル列で表全体を常に並べ替え、部分範囲の
  並べ替えは無い。安全側の割り切りだが本家と挙動が違う。
- ✔(2026-08-07 修正: 一覧に ✓ と状態行の「今の書式」)**[中] 数値書式の現在値表示** — 本家はホームのコンボが選択セルの書式種別に追従
  (controller/Toolbar.js:3670-3678)。うちは一覧を開くだけで、今の書式が画面に
  出ず、一覧にも印が無い。
- ✔(2026-08-07 修正)**[低] 固定の「固定された枠に影を付ける」** — 本家 ViewTab.js:135 freezeshadow。
  うちの固定の一覧(4項目)に無い。
- ✔(2026-08-07 修正: calcPr 往復)**[低] 計算方法(手動)がファイルに残らない** — xlsx の calcPr calcMode を
  読み書きしていない。手動にしても開き直すと自動に戻る。
- ✔(2026-08-07 修正)**[低] 入力規則セルの ▾ が押せない** — calc/src/view.rs の右下 ▾ は目印だけ。
  本家はクリックで一覧が開く(うちは右クリック→ドロップダウンから選択)。
- ✔(2026-08-07 修正: 値が複数のとき3択で聞く)**[低] 結合の警告なし** — 本家は複数の値がある範囲の結合前に警告。うちは黙って
  結合する(ただし値は消さない設計 — 解除で戻る)。
- ✔(2026-08-07 修正: hide_arrow として往復+▾の表示に反映)**[低] dataValidation の showDropDown 属性を読み飛ばしている**(▼を出さない指定)。

問題なしを確認した点: 絞り込み中の▼は色が変わる(view.rs flt 釦の active)、
消去5択の対象は本家一致、freeze のプリセット4項目の意味は本家一致。

## ピボットの残り(2026-08-07 — 本家画面との突き合わせで発注者と確認)

- ✔(2026-08-07: ピボットタブ「フィールドリスト」— ✓入りの4段に読み込み、同じ場所に置き直す。総計などの性質は据え置き)**[大] フィールドリスト(あとから行・列・値を組み替える)** — 本家はピボット
  タブの「フィールドリスト」で右パネルが開き、行・列・値を後から入れ替えられる。
  うちは挿入時の4段の聞き取りだけで、組み替え=作り直し。
  実装案: ピボットタブに「フィールドリスト」— いまの指図を ✓ 入りで4段に
  読み込み、集計まで選んだら**同じ場所に置き直す**(spawn_pivot の replace)。
- ✔(2026-08-07: 見出しの ▼ → ☑/☐ の一覧 → 決定で polars に filter を掛けて置き直し。指図の hide として joPivot.xml に往復。絞り込み中は ▼ が白地)**[中] ピボット内の絞り込み(見出しの ▼)** — 本家は行・列の見出しに ▼ が
  付き、項目を選んで絞れる。うちの ▼ はオートフィルタ専用。
  polars 側の filter と指図への控えが要る。
- ✔(2026-08-07: ピボットタブ「スタイル」— 青(既定)/緑/橙/灰 の4組。指図の style として往復)**[小] スタイルギャラリー** — 本家は複数のピボットスタイルから選べる。
  うちは1種(青の帯)。色違いを数種、一覧で選ぶ形が相場。
