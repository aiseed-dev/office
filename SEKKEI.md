# office — ネイティブ Rust の日本語オフィス(設計)

## Office を一つのソフトにしない(2026-08-02 確定)

文書は **writer**(docx互換)、表は **calc**(xlsx互換)。**別のバイナリ**にする。
共有するのは書式(docx/xlsx)と核(kumihan/sheet)だけで、アプリは分ける。
巨大なスイートにしないほうが、小さく作れて検査もできる。

範囲は「Euro-Office ができている程度」で十分。凝った機能は要らない。
**VBA 型のマクロは実装しない** — 「開く=実行」という攻撃経路を持たない
(aiseed-migration-kit DESIGN.md §5 と同じ思想)。コードの持ち運びは
Python in Calc が担う(2026-08-04 改訂): ブックにコードを**載せられる**が、
開いても決して自動実行せず、実行は明示の操作+檻(サンドボックス)の中だけ。
詳細は「Python in Calc」の節。

## 前提の確定(2026-08-02)

- Euro-Office のデスクトップ版(DesktopEditors)は存在するが**遅い**。
  遅さはバグではなく構造 — JSエディタ + Chromium という Web スタックそのもの。
  シェルだけ Rust にしても(desktop-sdk の置き換えでも)この遅さは継承される
- **Zed が証明したこと**: 「Rustで書けた」ではなく、**速さは構造で決まる**。
  GPU直描きのUI(GPUI)、ロープ構造の文書、ネイティブのテキストシステム。
  GPUI は Apache-2.0 で公開されており、その土台の上に建てられる
- したがって蒸留の最終形は、シェルではなく**オフィスそのもの**をネイティブで書くこと

## 構成(Zed との対応)

| Zed | office | 状態 |
|---|---|---|
| rope / text system | **kumihan**(組版エンジン: 文書モデル + JIS X 4051 行組み) | K0 実装中 |
| GPUI(GPU UI) | GPUI 採用が第一候補(代替: wgpu + vello/parley 系) | K1 で可否検証 |
| tree-sitter / LSP | OOXML 読み書き(docx⇄自前モデル) | K3 |
| — | **日本語一級**: 縦書き・ルビ・均等割付(存在理由) | K4 |
| — | 表・様式(罫線帳票)・印刷。yoshiki(様式プロファイル)が合流 | K5 |

## 原則

- **核はUIに依存しない。** kumihan は紙面(置かれた文字の座標)だけを返す。
  画面も PDF も印刷も、同じ紙面を別の面に写すだけ — 画面と紙が一致する
- **互換は書式の境界で守る**(docx を読み書きする)。エンジンは継がない。
  vendor/ の web-apps・sdkjs・desktop-sdk・DesktopEditors は**挙動の教師**(参照資料)
- 検査できる単位で進める。里程標(K0〜K5)ごとに受入条件と自動テスト
- 難所を先に言う: **IME**(K2。カスタムUIでの日本語入力は歴史的な難所)、
  **OOXMLの広さ**(K3。全部は実装しない — 読めないものは読めないと言う)

## リボン(2026-08-02)

タブ名と並びは **Euro-Office に合わせる**(乗り換える人が場所を覚え直さずに済む)。
名前は vendor/web-apps の日本語ロケール ja.json から取った。定義は `ui/src/ribbon.rs`。

- writer: ファイル / ホーム / 挿入 / 描画 / レイアウト / 参考資料 / フォーム /
  ヘッダー・フッター / レビュー / コラボレーション / 保護 / 表示 / プラグイン
- calc: ファイル / ホーム / 挿入 / 描画 / レイアウト / 数式 / データ /
  ピボットテーブル / 表のデザイン / コラボレーション / 保護 / 表示 / プラグイン
  (全タブ入り済み 2026-08-04。フォーム/ピボットテーブル/表のデザインは灰色置き、
  例外は表のデザインの「重複データを削除」= データタブと同じ実体、そして
  「ピボットテーブルの挿入」= polars 裏方で動く(発注者確定 2026-08-04)。
  結果はその時の値で右に置く — 開く=再計算の仕掛けは持たず、帳面には
  値だけが残る。同日、タブの残り(更新・すべて更新・選択・総計・小計・
  空行・レイアウト)も実装 — 指図(PivotDef)を独自部品 xl/joPivot.xml で
  ブックに控え、どの釦も「指図を直して polars で置き直す」だけ。
  総計・小計は polars が元データから計算し直すので平均でも正しい。
  Excel で保存し直すと指図は消え、ただの値になる(正直な劣化))

**改訂(2026-08-04 発注者確定): メニューは制限せず全部入れる。**
共同編集・保護・プラグイン(マクロ含む)のタブも、場所は本家どおり並べる —
乗り換える人の目当てを消さない。**実装するかは別の判断**で、当面は灰色
(「できないものを、できるように見せない」は ready で守る)。
VBA 型マクロを実装しない方針は不変(コードの持ち運びは Python in Calc が
檻つきで担う)。共同編集・保護は実装の相談事項として残る。

**できないものを、できるように見せない。** 実装済みだけ押せる形にし、未実装は灰色で残す。
並びは Euro-Office のまま、進み具合(実装済み N/M)が画面に出る。
テストで `ready` と実体の食い違いを止めている。

## 校正 — ローカルの Command A+(`ui/src/proof.rs`)

日本語の「スペルチェック」は綴りの話ではない。実際に起きるのは**誤変換**(以外/意外)・
**表記ゆれ**(問合せ/問い合わせ)・送り仮名・重複表現で、辞書式では検出できない。
だから中身にモデルを使う。**これは「AI機能」ではなく、校正という普通の機能の実装手段** —
だからリボンに AI タブは作らず、レビュー > 校正 に置く。

- 宛先は 127.0.0.1 のローカルサーバ(OpenAI互換 `/v1/chat/completions`)。
  **基幹網は外に出ない**ので、閲覧の分離設計と矛盾しない。
  `JO_PROOF_HOST` / `JO_PROOF_PORT` / `JO_PROOF_MODEL` で差し替え
- Command A+ はオープンウェイト(migration-kit DESIGN.md でローカルAIとして採用済み)。
  ただし大型なので相応の GPU メモリが要る(同 DESIGN.md の注記)
- **モデルの作り話は通さない**: `found` が本文に実在しない指摘は捨てる
- **サーバが居なければ「校正できません」と出す。** 黙って「指摘なし」にしない
  (利用者が「誤りが無い」と受け取ってしまうため)

## なぜこれを作るのか — 英語は解放された。日本語は解放されなかった

英語の綴り検査は**公共財になっている**。この機械で確かめた:

    hunspell-en-us は システムパッケージ。
    依存元: libenchant-2-2, task-gnome-desktop, task-kde-desktop,
            task-xfce-desktop, task-mate-desktop, task-lxqt-desktop …
    語彙: SCOWL(Kevin Atkinson)・Moby lexicon・Princeton WordNet

**誰も英語を正しく綴るために Microsoft に金を払っていない。** 辞書は誰でも同梱できる。

日本語には対応物が無い。`/usr/share/hunspell/` に ja は無く、`apt-cache search hunspell`
にも日本語の辞書は出てこない。だから日本語の校正は、Word の校正・ATOK・Just Right! を
買うしかなく、いまそれがクラウド(Copilot)へ移りつつある。

**これは誰かの怠慢ではなく、構造。**

| | 誤りの正体 | 辞書で解けるか |
|---|---|---|
| 英語 | 綴り誤りは**辞書に無い語**になる (`recieve`) | 解ける → 公共財になった |
| 日本語 | 誤変換は**辞書に有る語**になる (「以外」/「意外」) | 解けない → 企業のものであり続けた |

ふりがなも同じ。「後」の読みは辞書に書けない(文脈で変わる)。
実測: 青空文庫5作・ルビ14,994箇所で、**読みが割れる語が561種**。

**オープンウェイトのモデルは、日本語にとっての hunspell になれる。**
hunspell が英語にしたこと(誰でも同梱できる・金を払わない・手元で動く)を、
日本語でやる。それがこのソフトの存在理由であり、
**言語の正しさの土台を一社から借りている状態を終わらせる**ということ。

## 辞書は答えではなく絞り込み(2026-08-02 に測り直した)

「英語は辞書で足りる」は**不正確だった**。辞書が答えられるのは
「この語は一覧にあるか」までで、**無い語が誤りかどうかは答えられない**。

    『高慢と偏見』 123,688語 → 辞書に無い語 312種 (0.25%)
    AMDの技術文書   2,114語 → 辞書に無い語  74種 (3.50%)

    例: Bennet, Bingley, Bakewell, Annesley … / DeepSeek, Docker, Franka, LangChain …

**全部、正しい語。** 辞書だけだと『高慢と偏見』に2,251件の「綴り誤り」を報告する。

だから配置はこうなる。**辞書は前置きの濾過器**で、残った未知語だけモデルに訊く
(固有名詞か、誤りか)。組み込みの固有名詞リストは誤魔化しなので、判定に置き換えた。

    英語  : 辞書が 99.7% を落とす → 0.25% だけがモデルに届く
    日本語: 辞書が何も落とせない  → 100%  がモデルに届く

**これが「日本語にGPUが要る」の定量的な意味**であり、同時に推論の最適化でもある
(一番速い推論は、動かさない推論)。同じ処理の流れで、濾過器の効きだけが違う。

判定の規則: **迷ったら name。** 正しい語を「誤り」と言う害の方が、
誤りを見逃す害より大きい。判定できなければ「綴り誤り」ではなく**未知語**として出し、
判定できなかったことを `skipped` に残す。一度判定した語は覚えて二度訊かない。

## 校正は office の一部。別ブランドで配らない

hunspell が英語で勝ったのは**誰でも同梱できたから**だが、それを可能にしたのは
**免許**であって、別売りのパッケージではない。だからこちらは
**aiseed office(仮称)として配り、校正はその中の機能**にする。
別ブランドの製品を増やさない。

**AGPL なので、単体で切り出して配りたい人は配れる。** こちらがやる必要はない。

`office-spell` と `furigana-bench` は残すが、これは**製品ではなく道具**である。
理由は1つで、**画面を開かずに回せないと測れない**から —
青空文庫17,000作品の命中率も、Radeon 上の推論速度も、窓越しには測れない。

    office-spell 文書.txt        指摘を出す(標準入力も可)
    office-spell -l              語だけ(hunspell -l と同じ形)
    office-spell --washi         第1候補を振った本文を pywashi の記法で出す

終了コードで区別する — **0=指摘なし 1=指摘あり 2=入力が読めない 3=検査できなかった部分がある**。
3 が要るのは、モデルが居ないときに 0 を返すと
「終了コード0 = 問題なし」と受け取られるため。**黙って「指摘なし」にしない**を
コードの形で守る(`Report::may_say_clean()`)。

固有名詞は一般の英語辞書に載らない(実際に `Radeon` → `Radon` と直された)。
hunspell と同じくユーザ辞書 `OFFICE_DICT_USER` で受ける。


## 名前と免許

**aiseed office(仮称)。** 「japanese-office」をやめたのは、
日本語が特別なのではなく**日本語が第2の型の言語の一例**だから。
`lang` の `Language` を1つ足せば各国語版になる形にしてある。

免許は **AGPL-3.0-or-later**。理由と代償は `NOTICE.md` に書いた。
要点だけ: **クラウドの役務として提供されたときに原本が渡らない穴を塞ぐ**ため。
大手のソフトに取り込まれる道は捨てる — 取り込まれるのではなく、置き換える。

`ribbon.rs` は Euro-Office(AGPL-3.0)の翻訳の派生物なので、由来の表示が要る。

## calc の分業 — Python に回すもの(2026-08-03)

境界線は一本:
**見ながら整える仕事は calc、データを作る・分析する仕事は Python。**

Python 側の道具は **polars を第一にする**(2026-08-03 ユーザー判断)。

**改訂(2026-08-04 発注者)**: Python の方が楽なものは、**リボンの釦の裏方として
Python を呼んでよい**。「作らない」から「裏で Python が働く釦」へ。第1号がグラフ —
挿入 > グラフを挿入は、選択範囲を matplotlib(裏方)で棒グラフにして画像として
シートに浮かべる(機械の日本語フォントを matplotlib に登録して豆腐を防ぐ。
Python の探し方: JO_PYTHON → .venv/bin/python → python3。無ければ status で言う)。

| リボンにあるもの | Python での扱い |
|---|---|
| グラフ | **釦あり**(裏方 matplotlib。上記) |
| 図形 | **釦あり**(発注者案 2026-08-04: **SVG で作る** — モデルは種類+色+錨で持ち、
  画面は大きさを織り込んだ SVG を都度ラスタ化(拡大しても鮮明)、xlsx へは
  DrawingML の prstGeom で書く = Excel でも本物の図形。読みは prstGeom の
  部分集合を SVG に写して表示、知らない図形は原文持ち越しのまま表示だけ諦める) |
| SmartArt | **釦あり**(2026-08-04 発注者提案「ONLYOFFICE からデータを取得したら」:
  分類・並び・日本語名は web-apps の define.js + ja.json の現物から。挿入は
  **うちの図形(SVG 方式)の集まり**として組む — 各図形が普通に動かせ、
  xlsx へは prstGeom で入る。本物の SmartArt 部品(diagram XML)は作らない。
  一覧には組める形だけを出す。図形に錨セルからの px のずらし(dx/dy)を
  新設し、oneCellAnchor の colOff/rowOff と往復 — セルの粗さに縛られず組める) |
| ソルバー / ゴールシーク | **釦あり**(2026-08-04 発注者確定: ソルバーの入力画面は
  ONLYOFFICE の「ソルバーのパラメータ」と同じ形の小窓。解法も本家と同じ
  **単体法 LP だけ** — 係数は表の複製から測り(全0→単位ベクトル→全1の
  検算)、線形でなければ正直に断る。解くのは scipy.optimize.linprog(highs)。
  ゴールシークは割線法で実装済み) |
| テキストからデータ / 区切り位置 | polars.read_csv(SJIS は先に文字コードを直してから) |
| 外部リンク(他ブックの参照) | 取り込みは Python でやり、値として渡す(リンク切れの帳票を作らない) |
| (ピボット集計・予測・統計) | polars の group_by・pivot / statsmodels |

- ボタンは**灰色のまま残す**(Euro-Office の並びは崩さない)
- 開いた帳票にあるもの(グラフ・図形・外部参照)は**保存で壊さない**(原文持ち越し済み)

### calc に残すもの(Python に出さない)

- 帳票の形: 罫線・結合・書式・列幅・行高・印刷 — 様式は見ながら作るもの。
  印刷の設定(2026-08-03): pageSetup / pageMargins / Print_Area は
  **モデルに読む**(向き・用紙・四辺の余白・印刷範囲)。UI とPDF が使い、
  保存は**原文への属性差し替え**(拡大縮小・印刷機の参照など理解しない属性を
  消さないため。丸ごと作り直すと「書式は据え置き」が破れる)。
  用紙コードの B は JIS(ECMA-376 の表は ISO だが日本の実情は JIS —
  ここは日本の事務様式のためのソフト)
- 式の再計算: 開いた見積書が正しく計算されるのはアプリの責務
- 並べ替え・フィルター: 見ながらやる操作
- データの入力規則・条件付き書式: 入力の現場で効くもの(**済** 2026-08-03。
  条件付き書式は cellIs、入力規則は list を読み・効かせ・往復。
  入力規則の候補は formula1 の**原文**で持つ — 範囲参照 `$D$2:$D$5` の中身が
  変われば候補も追従する。規則に合わない入力はセルに入れず、Esc で戻す。
  候補が解決できない規則(別のシートへの参照等)では**堰き止めない** —
  読めない規則で利用者を閉じ込めるより、通して警告しない方を取る。
  list 以外の型は Report へ(保存で失われる、と言う))

### 端末のソフトの土台として(2026-08-04 発注者)

「これで端末のソフトはいくらでも作れる」— 二つの意味で正しく、二つ目が本命。

**1. 新しいバイナリの土台として。** 難所は一度だけ解けばいい。IME・日本語
フォント・GPU 描画(GPUI)・印刷/PDF・リボン・ダイアログの別糸化・undo の
規律・排他ロック・Python 拡張(檻つき)・仮想画面での自動検証 — 全部が
共有クレート(kumihan / sheet / paper / ui)と作法(SEKKEI・家訓・
wiring_tests)に貯まっている。3本目のソフトはこの上に薄く載るだけ。

**2. 多くの業務ソフトは、もう「作らなくていい」。** Python in Calc +
檻 + ファイルサーバーで、**xlsx 1つが業務アプリそのもの**になった。
帳票(UI)+ コード(ロジック)+ 運用(状態列と入力規則)が1ファイル —
問い合わせ・顧客・在庫・勤怠・日報・検収、事務の台帳系はほぼ全部この形に
収まる。共有フォルダがそのまま配布網で、導入も更新もファイルコピー。
**「いくらでも」の実体は、N 本のバイナリではなく N 個のブック。**

**改訂(2026-08-04 発注者確定): 新しいバイナリは作らない。** writer と calc の
2本で打ち止め。以後の「新しいソフト」はすべてブック(帳票+コード+運用)で
作る。UI が足りなければ、バイナリを増やすのではなく **calc / writer 側を
伸ばす**(伸ばした部品は全ブックの共有財産になる — 図形・グラフ・UDF が
そうだったように)。バイナリを増やしたくなったら、それは**範囲の外の合図**
(このソフトの仕事ではない)。バイナリが2本で固定なら、検査・配布・保守の
対象も2本で固定 — 「いくらでも作れる」と「保守が貯まらない」が両立する。

限界も書いておく: 保守は貯まる(GPUI の上流追従、この規模を発注者+Fable で
回し続ける前提)。檻は OS ごとに組む — Linux は bubblewrap(済)、Mac は
Seatbelt(「Mac 版」の節。当初は「Linux 専用」と割り切っていたが
2026-08-04 に改めた)。檻が組めない環境ではブック搭載コードを実行しない、
という規律が両方を守る。様式と業務知識は結局それぞれの現場の仕事。

### ファイルサーバーの現実に合わせる(2026-08-04 発注者)

多くの現場はクラウドに移れない(閉域網・規程・データ主権)。基幹は
**共有フォルダの xlsx** であり続ける。この設計はその現実のためにある:

- 帳票・コード・運用が**1つの xlsx** で共有フォルダに置ける(Python in Calc)。
  サーバーは物置のままでよく、Web アプリも DB も SaaS 契約も要らない
- 共有フォルダ = 他人のファイルが日常、という脅威にはニ重の防壁
  (自動実行なし+檻)が正面から答える。VBA が背負っていた危険の置き換え
- **排他**(済): 開くとき `.~lock.名前#`(LibreOffice と同じ場所)を見て、
  先客が居れば名乗りを見せ、**上書き保存を止める**(名前を付けて保存へ)。
  居なければ自分のロックを置き、閉じる・別のファイルへ移るときに外す。
  「二人で開いて後勝ちで潰す」という共有フォルダの定番事故を防ぐ。
  版の履歴・バックアップはファイルサーバー側の仕事(スナップショット)
- writer も同じ排他が要る(未実装 — 相方の課題として HIKITSUGI へ)

### Python in Calc(済 2026-08-04 発注者提案。同日、コードの持ち運びまで拡張)

データ > Python。一行のコードか .py ファイルを、**b(ブック)と s(いまの
シート)を束縛して**回す。裏はいまの表を一時 xlsx へ写し(原本の部品ごと)、
office_sheet(pysheet)で実行し、保存されたものを読み戻して**1手として**
適用する(undo は束 — 複数シートに触っても Ctrl+Z 一回で戻る)。
失敗しても表は無傷(実行は複製の上)。

**コードはブックに載せて持ち運べる**(発注者判断 2026-08-04)。
`@save 名前` で .py をブックに載せ(xl/joPython.xml。この形式の独自部品)、
`@名前` で実行、`@list` `@del 名前`。帳票とコードが一緒に旅をする —
マクロの利便はこれで得る。そのうえで**二重の防壁**が旧原則の核を守る:

1. **開いても決して自動実行しない。** 実行は常に明示の操作(@名前)だけ。
   「開く=実行」という攻撃経路は今までどおり存在しない
2. **ブックに載っていたコードは必ず檻(bubblewrap)の中で回す** —
   他人のファイル由来かもしれないから。檻: ネットワーク遮断・実ファイルは
   読み取り専用・ホームは不可視(tmpfs)・書けるのは交換用の一時領域だけ。
   檻の無い機械では載ったコードは**実行しない**(そう言う)。
   自分で打った/選んだコードも、檻があれば檻で回す(深層防御)

Python in Excel との違い: あちらはクラウド実行(データが Azure へ出る・
従量課金)。こちらは**手元で動き、基幹網の外に出ない**。polars・scipy・
matplotlib など機械にある物が全部使える。Excel は未知の部品を保存で
落とすことがあるので、Excel で開いて保存し直すと載せたコードは消え得る
(コードは消えても値は残る — 劣化は安全側)。

**UDF とスピル**(済 2026-08-04。発注者の 2018 年の Qiita 記事
「Excel に Python が搭載?その後 — xlwings を使おう」が前史。xlwings の
目玉だった「Python で書くセル関数」を、8年後に自分の calc へ本搭載):
- セルに `=PY("関数名", 引数…)`。関数の定義はブックの**「関数」で始まる
  名前のスクリプト**(@save 関数)にある def
- **PY セルは開いても再計算でも実行されない。** 最後に計算した値を保ち、
  未計算なら #PY? の印。評価は板の `@計算` のときだけ、**網なし檻**の中で
  一括 — 「開く=実行を持たない」はセル関数でも破らない
- 2次元の返り値は右下へ**スピル**。展開先に他人のデータや式があれば
  #SPILL! で止まる(潰さない)。縮んだときは前回の面の残骸を消す
- 引数は式をいま評価した値で渡す(範囲は列数つきの2次元)。式は保たれる
  ので、@計算 を押し直せば新しい引数で再評価される
- Excel で開くと PY は #NAME? になる(値は保存されているので、Excel が
  再計算するまでは見える)。劣化は正直に

**檻は段階制**(2026-08-04。Web の問い合わせ取り込みのため):
- 自分で打った/選んだコード: **網あり檻**(ファイルは守られたまま、網は通る —
  自分の道具が Web から取り込むのは普通の仕事)
- ブックに載って旅してきたコード(@名前): **網なし檻**が既定
- `@名前 net` と**その場で打ったときだけ**網あり檻 — 許可は毎回の明示の
  操作で、**ブックには決して保存されない**(ファイルが自分に権限を
  与えることはできない)

**使い所の実例 — Web の問い合わせ台帳**(実証済み): フォームの受信箱
(API / CSV)を取り込むコードをブックに載せ、`@取り込み net` で台帳に
「受信・名前・用件・状態=未対応」を追記する。帳票・コード・運用が
1つの xlsx で旅をする。網なし檻では同じコードの通信が拒否されることも実証。

配り方: `cargo build -p pysheet --release --features extension-module` の
`liboffice_sheet.so` を **office_sheet.so の名で calc の隣に**置く。
無ければ status がその手順を言う。

### 分業の橋 — `pysheet`(済 2026-08-03)

`sheet` を Python から呼べるようにした(pyo3, crate は `pysheet`、
import 名は `office_sheet`)。openpyxl と違い、罫線・結合・列幅・図形を
保ったまま値を差し込める — **polars で集計 → 帳票の枠を保ったまま差し込む**。
マクロの置き換えがスクリプトになる、という当初の設計と一直線。

```python
import office_sheet
b = office_sheet.Book.open("様式7.xlsx")
s = b["提案見積書"]              # b[0] でも
s["A30"] = "日本フネン株式会社"   # 書式は据え置き。文字列は「打ったのと同じ」解釈
s["C30"] = "=B30*100"            # 式。置いた時点で再計算される
s.insert_row(30)                 # 明細行を増やす。残った式の参照も動く
b.save("out.xlsx")               # 原本から図形・印刷設定を持ち越す
```

- 値は Python の型のまま(数=float, 文字=str, bool, 空=None)。
  `s.values()` は行ごとの list[list] — `polars.DataFrame(…, orient="row")` でそのまま表になる
- `b.unsupported` に読めなかったものの帳簿が出る。**黙って落とさない**はここでも
- 配る .so は `cargo build -p pysheet --release --features extension-module`
  (abi3-py310: libpython 非依存で、どの Python 3.10+ でも同じ .so が動く)。
  extension-module を既定にしないのは cargo test がリンクできなくなるため(pyo3 の FAQ)
- 検査は `pysheet/test.py` を Rust の試験(`tests/python_smoke.rs`)が回す。
  実物の様式7で「差し込んでも結合が崩れない」ことまで見る
- polars 連携は実地で確認済み(2026-08-03): miniforge の `.venv`(Python 3.14 +
  polars 1.43)で、**3.12 で組んだ abi3 の .so がそのまま動く** — group_by の集計を
  実物の様式7に差し込み、保存しても結合・元の中身は無傷。test.py の polars の節は
  polars がある環境でだけ回る(無ければ飛ばしたと言う)

### writer(docx)には橋を作らない(2026-08-03 確定)

**全部 Rust で書く必要はない**(ユーザー判断)。xlsx に pysheet が要ったのは
openpyxl が帳票の枠(罫線・結合・列幅)を壊すからで、docx は事情が違う —
**python-docx は XML の木をその場で書き換える**作りなので、
理解しない部品(画像・スタイル・ヘッダー・フッター)を最初から壊さない。
公共財が既にあるところには作らない(hunspell と同じ論理)。

実物の様式1(参加表明)で確認済み:
- python-docx 1.2.0 で記入して保存 → 本当に変わったのは word/document.xml
  (編集した本文)だけ。styles・footer・settings は再直列化で字面が変わるが
  **中身は同一**(C14N で比較)。[Content_Types].xml も宣言の集合は同一
- **相互運用**: python-docx で埋めた様式を、こちらの ooxml(writer)が
  そのまま読める(unsupported 空)
- 記入の作法: **先頭ランに書き、残りのランを空にする**(段落の書式・書体が残る)

```python
import docx
d = docx.Document("様式1.docx")
p = d.paragraphs[12]
p.runs[0].text = "商号または名称　日本フネン株式会社"
for r in p.runs[1:]: r.text = ""
d.save("記入済.docx")
```

## writer の挿入系 — Python で描いて画像として貼る(2026-08-03 確定)

calc の分業と同じ線を writer にも引く(発注者確認済み)。
**文書の中で描く道具は作らない。絵は Python で描き、画像として貼る。**
このソフトは事務様式**専用ではない**(2026-08-03 発注者)— 汎用の
ワープロとして考える。ただし絵を描く場所は文書の外(Python)でよい。

| リボンにあるもの | 釦の動き |
|---|---|
| 図形・SmartArt・グラフ | 画像を選んで貼る(matplotlib / PIL で描いた PNG を想定) |
| テキストアート(飾り文字)・方程式 | 同上(方程式は matplotlib の mathtext 等) |
| テキストボックス | 1×1 の表を挿す(枠の中に文字が書ける。セル編集は実装済み) |
| ドロップキャップ・コンテンツコントロール | 未実装(灰色のまま。作らないと決めたわけではない) |

- **きちんと動作する釦は灰色で残さない**(2026-08-03 発注者)。
  挿入系は上の表のとおり「画像として貼る」「表を挿す」で実際に動くので、
  普通の釦として配線した。並びは Euro-Office のまま
- 貼る道: 「画像を挿入」が PNG/JPEG を受け、docx の部品
  (media/rels/[Content_Types])ごと書き出す。撮って出しの寸法は 96dpi、
  行長に収まらなければ比例で縮む
- 開いた文書にある図形・テキストボックス・グラフは**原文持ち越しで保存で
  壊れない**(画面には出ない。帳簿に「保存では残る」と出す)
- 実地検証(2026-08-03): PIL で描いた棒グラフの PNG を「画像を挿入」と
  同じ道で貼り、保存 → 開き直しで画像が残り、LibreOffice が PDF に
  画像入りで変換できることを確認(matplotlib はこの機械に無いが、
  PNG を作って渡す流れは同じ)

## writer の編集モデル — run 粒度(2026-08-04)

**1回の編集は、連続した1箇所の置き換え**(打鍵・削除・貼り付け・IME の
確定・undo の1手、すべてそう)。この不変条件が編集モデルの土台。

- `set_body_text` は、編集前後の本文の頭と尻尾の共通部分を除いて
  「その1箇所」を特定し、`splice_text` が run の構造へ正確に写す。
  旧方式(段落番号で段落をまるごと写す)は廃止 —
  **run の境(部分書式)・段落の性質・表の位置が、編集で流されなくなった**
  (旧方式の持病: 段落の増減で下の段落の性質がずれる/打鍵のたびに
  表が末尾へ動く — どちらも解消。テストで固定)
- 書式の適用(`apply_char_format` / `apply_size` / `apply_font`)は
  選択範囲の境で run を割り、**選択の字にだけ**掛かる。選択が空なら
  従来どおり段落まるごと(方向の癖を変えない)。端が字の途中なら
  その字はまるごと含める
- 段落を割る(Enter)と、両方の半分が段落の性質を持つ(Word と同じ)。
  合流(改行の削除)では頭の段落の性質が残る
- 差し込む字の書式は「置き換えの直前の字」のもの(打つとその書式に
  なる、の慣習)。`normalize_runs` が空の run を除き、同じ書式の隣を
  繋ぐので、編集で run が際限なく増えない
- 描画(画面・PDF)は**同じ書式の連なり**ごとに描く。Cell が書体も
  持ったので、行の中で太字・大きさ・書体・色が混ざってもその通りに出る
- **注意**: 一括の複数箇所置き換え(すべて置換)は「1箇所」の前提から
  外れるため、頭と尻尾の間の書式が均されうる。置換のたびに写せば
  避けられる(今後の改善点)
- これで**相互参照(REF フィールド)を本文で持つ土台**ができた —
  フィールドを run として持てば、編集で流されない

## writer の相互参照 — run が持つ参照(2026-08-04)

run 粒度の編集モデルの最初の応用。**run 1つが1つの参照**(REF / PAGEREF)で、
run の text が「いま見えている値」。

- 参照は書式(CharFormat.field)に持つ — run の分割・結合・描画の既存の
  道具立てがそのまま効く(field が違えば normalize で繋がらない、
  描画は連なりの境で切れる)
- **編集の規則**: 参照の中を割ったら普通の字に降りる(半分ずつが参照を
  名乗ると更新で二重になる)。参照の直後に打った字は参照を継がない。
  参照ごと太字にはできる(全体を覆う適用は分割しないので残る)
- 挿入は板(しおり一覧 → 文字/ページ)。値は挿した時点で計算し、
  「参照を更新」で計算し直す(更新は run の text を直に書き換えるので
  編集の平文を作り直す = **undo の控えが失われる。そう言う**)。
  保存時は写し(doc_for_save)でも更新するので docx の値は常に新しい
- docx とは fldSimple / fldChar の REF・PAGEREF で往復。見えている値は
  キャッシュとして入るので、Word/LibreOffice はそのまま表示も再計算もできる
  (LibreOffice が実際に再計算することを確認済み)
- 画面では参照にうっすら網掛け(Word の作法 — 「計算された値」と分かる)

## writer の変更履歴 — 差分を保存で刻む(2026-08-03)

編集モデル(平文+段落持ち越し)に操作の記録を足すのではなく、
**保存のときに「記録開始時点との差分」を計算して w:ins / w:del にする**。
編集中は普通に打てて(モデル無変更)、Word には本物の変更履歴として届く。

- レビュー > 変更履歴 = 記録の開始/停止。開始時点の段落の写しを持ち、
  画面では変わった段落の左に橙の棒(Word の変更バー)
- 差分は2段: 段落の列を LCS で突き合わせ(変更/新規/削除の段落を出す)、
  変わった段落の中は共通の頭・尻尾を残して間を del + ins に
- 印は私用領域の字(TRK_INS_S 等)で保存用の写しに埋め、ooxml が
  w:ins / w:del(著者つき)に変換する — PAGE フィールドと同じ作法
- 読みは**確定後の姿**(挿入は本文、削除は見せない)。履歴そのものは
  モデルに持たないので、**保存すると履歴は確定される — 帳簿でそう言う**。
  履歴の中身の閲覧・個別の採否は今後の課題
- 消えた段落は「全部削除の段落」としてその場所に置く(Word では
  取り消し線の段落として見える)

## writer の描画(ペン)— ページに固定した筆(2026-08-03)

- 筆(Stroke)は**ページ番号 + ページの中の mm 座標**で持つ。本文を編集しても
  動かない — Word のページ固定の図形と同じ性質。画面と紙の対応は
  paper::paginate の折り方(ページの繰り上げ量)で取る
- docx では**自由曲線の図形**(DrawingML custGeom、ページ基準の anchor)。
  ページ番号は図形の名前(joink{id}p{page})で持つ — XML の座標は
  ページの中の位置しか持てないため。開くときは原文控えから筆へ読み戻す
  ので、このアプリ同士なら消しゴムも効き続ける。Word/LibreOffice でも
  普通の図形として見える(LibreOffice の描画を画素で確認済み)
- 蛍光ペンは太く・薄く(alpha 45%)・文字の下(behindDoc)。ペンは細く・
  濃く・文字の上。画面の描画は gpui の Path(塗り)で、折れ線を
  「幅のある四角形の連なり」として塗る
- 保存は**写し**に差し込む(doc_for_save)— モデル本体に足すと
  保存のたびに図形が増える。undo は筆の一手ずつ(道具を持っている間の
  Ctrl+Z は筆の取り消し)

## writer の見出しと目次(2026-08-03)

- 段落の役割は `ParaStyle`(Body / Heading 1〜3 / Toc 1〜3)。
  docx の `w:pStyle` と往復する。見出しの style id は日本語版 Word が「1」、
  英語版が「Heading1」— 読みは両方受け、書きは `Heading{n}` + `outlineLvl`
  (スタイル定義の無い文書でも見出しとして扱われるように)
- **スタイル定義(styles.xml)は持たない。** 見出しを付けるとき、見た目
  (太字・大きさ)は直接書式で付ける。開いた文書の styles.xml は今まで通り
  原文で持ち越す
- 目次は**静的な本文**として挿す(TOC フィールドは書かない — 開いた側に
  「フィールドを更新してください」を出させない)。目次の行は
  `ParaStyle::Toc` の印(docx では pStyle TOC1〜3)を持ち、
  「目次」「目次の更新」はどちらも Toc の連続を丸ごと作り直す。
  挿すときは**編集(undo の1手)と blocks を同じ形で揃える** —
  揃えないと set_body_text の性質の持ち越し(段落番号ベース)がずれる
- ページ番号は `paper::paginate`(to_pdf と同じ折り方に一本化)から出す —
  **目次の番号と印刷した紙が食い違わない**

## writer のヘッダー・フッター(2026-08-03)

- **モデル**: `Document.header / .footer = HeadFoot`(段落の列 + 読み込んだ部品名)。
  **paragraphs が空 = 持っていない/持てなかった**。空のヘッダーは空の段落1つで表す。
  この区別で「触っていない部品を保存で書き潰さない」を守る
- **ページ番号**は私用領域の1字 `PAGE_MARK`(U+E000)として文中に持ち、
  docx とは PAGE フィールドで相互変換する(fldSimple と fldChar の両方を読む。
  separate〜end の「計算済みの見た目」は捨てる — 開く側が計算し直すもので、
  持ち越すと古い番号が本文に化ける)。PAGE 以外のフィールドは報告して落とす
- **編集は板方式**(writer の検索欄と同じ)。紙面の上で直接編集しないのは、
  番号への置き換えでバイト位置が変わり、キャレットが嘘をつくため。
  板では印を《ページ番号》と見せる
- **描画**: 画面の紙は巻物なので、ヘッダーは紙の頭・フッターは末尾の頁の位置に
  灰色で出す(番号は1ページ目のもの)。**本当の番号は紙(PDF)で入る** —
  `paper::to_pdf_with(page_decor)` が頁ごとに `kumihan::layout_hf` の行を置く
- **保存**: 原本に部品(headerN.xml)があれば**同じ名前へ書き戻す**
  (参照・rels・[Content_Types] は原本のまま)。無ければ johdr1/joftr1 を作り、
  sectPr に参照を差し込む。**表入りのヘッダーは編集の対象にしない**
  (モデルが表を持てるようになるまで、壊すより原文を生かす。開いたとき報告する)

## writer の保護と共同編集(2026-08-04)

発注者確定: コラボレーション・保護・プラグインの釦も灰色のままにしない。
順に実装する。まず「保護」と「共同編集モード」。

- **保護(prot-doc)** = docx の `documentProtection`(settings.xml)の
  `w:edit="readOnly"` と往復する読み取り専用。効き目は本物:
  打鍵は on_edited の入口で取り消され(undo で文書と一致するまで戻す)、
  文書を変える釦は run_cmd の白表(READONLY_OK)で断る。
  見る・出す・保存・検索は通す。Tab のインデントは run_cmd を通らないので
  para() にも同じ番人を置いた
- **パスワードは掛けない(掛けた振りもしない)**。Word のパスワード無し
  「編集の制限」と同じで、解除は誰でも同じ釦1手。ハッシュ照合だけの
  保護は錠前ではなく注意書き — そう見せる
- **バージョン履歴(co-history)** = 上書き保存の前に直前の中身を
  `.jo-history/<名前>/<mtimeの日時>.docx` へ控える(9世代)。板から押すと
  **名無しの複製**として開く — 元のファイルを黙って書き戻さない
  (戻すなら同じ名前で保存する、を人に選ばせる)
- **チャット(co-chat)** = 文書の隣の申し送り帳 `名前.docx.chat.txt` に
  名乗り(user@host)と日時つきで追記する言伝。サーバーは無いので
  生放送ではない — そう見せもしない
- **マクロ(plug-macros)** = calc の Python と同じ檻(bubblewrap)の中で、
  python-docx が文書の**複製**を直し、直った複製を読み込む(台本の `d` が
  文書)。文書にコードは載せない — 「開く=実行」を作らない設計はそのまま。
  戻すのは Ctrl+Z の1手(doc_undo に直前の文書を控える)
- **プラグインの管理(plug-manage)** = `~/.config/office/plugins` の .py を
  一覧し、マクロと同じ檻で実行するだけの素直な置き場方式
- **暗号化(prot-encrypt)** = ECMA-376 Standard Encryption(AES-128・
  SHA-1 50000回・CFB 容れ物)を `ooxml::crypt` に自作。Word 2007 以来の
  「標準」方式で、書いたものは msoffcrypto-tool(実物の Word 暗号を解く
  道具)で解けることを実地で確認済み = 形式が本物。Word 2013+ の既定の
  Agile 方式は**まだ読めない — 読めないと言う**(黙って壊さない)。
  開くときはパスワードの板(伏せ字)で聞き、保存も同じ鍵で包み直す。
  原本の部品持ち越し・マクロ・バージョン履歴も解いた平文で回す
- **デジタル署名(prot-sign)** = **隣の .sig への添え書き**(Ed25519。
  鍵は ~/.config/office/sign.key に自動生成・0600)。押すと 有効なら報告、
  無い・中身が変わったなら署名し(直し)。Word の署名欄に出る XML-DSig
  ではない — そう言って出す(改ざん検知と名乗りが実体。本人確認の
  基盤は持たない)。書式は外部の道具でも検証できることを確認済み
- **共同編集モード(coauth-mode)** = 新しい仕組みは作らず、既にある
  ファイルの錠(`.~lock.名前#`)の顔。押すと錠の今を名乗り付きで見せ、
  先客が去っていれば編集権を取り直す。サーバーの無い共同編集は
  「早い者勝ちの編集権 + 後の人は読むだけ」が正直な形

**calc も同じ機構で全部実装済み(2026-08-04)。** 違いは2つだけ:
保護は docx の documentProtection ではなく **xlsx の sheetProtection**
(シート単位。編集の堰は commit / fmt / 貼り付け / Del / run_cmd の白表
PROTECTED_OK)。マクロは python-docx ではなく **Python in Calc と同実体**
(b=ブック s=シート)。暗号化・署名・履歴・チャット・錠の顔は共通の
仕組み(ooxml::crypt / .sig / .jo-history / .chat.txt)をそのまま使う。

## 画面の額縁 — デスクトップ版に合わせる(2026-08-04 発注者)

発注者がデスクトップ版 ONLYOFFICE の画面を示して「これに合わせて」。
writer で実施(calc への写しは申し送り):

- **1段目 = クイックアクセス+文書名**。保存・印刷・戻す・やり直しの
  アイコンを左に、中央に文書名(未保存は * 印)。この行が窓の取っ手
- **2段目 = タブ**。白地に文字、現在地は青い下線(タブ形の色分けを廃止)。
  右端に 🔍(検索と置換の板)
- **下端 = ステータスバー**。ページ n/m・文字数(生きた値)・状態の文言・
  スペル・ズーム −/%/＋。レビュータブのスペルと文字カウントの引っ越し先
- **タブの構成もデスクトップ版のとおり**: 「コラボレーション」は
  デスクトップ版の呼び名「共同編集」に(変更履歴もそこへ)。
  ヘッダー/フッタータブは畳んで挿入タブへ、レビュータブは畳んで
  共同編集とステータスバーへ。フォームタブは残す(発注者の
  「メニューを制限せず全部入れる」の確定を優先。デスクトップ版は
  docx では出さないが、うちは灰色=未実装の正直表示として残す)
- **ホームタブは本家の形**(2026-08-04 発注者が画像で指定): 2段組・
  絵だけの釦・群の区切り線・フォント名と大きさはコンボ風(今の値を
  見せる)。クリップボード(コピー/切り取り/貼り付け)と左右揃えの
  釦も本家どおり追加(実体は Ctrl+C/X/V と set_align)。絵だけになるので、
  **釦に乗るとステータスバーに名前が出る**(hover_hint)。
  足りない絵(箇条書き・左右揃え・非表示文字)は同じ流儀で自作
  (icons.rs の OWN_ICONS — extract_icons.py の再生成で消さないこと)
- **挿入タブは一段**(2026-08-04 発注者が画像で指定): 主要な釦
  (空白ページ・区切り・表・画像・図形)は絵の下に短い名札、他は絵だけ。
  タブごとの配置は writer の LItem 表(HOME_ROWS/INS_ROWS)で持つ。
  足りない絵(表・画像・図形・SmartArt・グラフ2種・テキストボックス・
  ヘッダー・フッター・ページ番号・ページ数)も OWN_ICONS に自作で追加
  (画像の釦は slot 名 insertimage に合わせて登録)
- ルーラーは既定で表示(デスクトップ版の見た目)

**calc へも写した(2026-08-04)。** 1段目はクイックアクセス+ブック名
(表計算の app 色 = 緑の帯。未保存は * 印)、2段目は白地のタブ+緑の下線+
右端 🔍。下端のステータスバーはシートの耳と同居し、状態の文言に加えて
**選択の生きた値(合計・平均・個数)**を出す — Excel の下端と同じ、
writer の文字数に当たるもの(全選択のような巨大な矩形では数えない)。
保護中のシートの耳には 🔒 が付く。数式バーはそのまま(表計算の顔)。

## Mac 版(2026-08-04 発注者)

Linux 前提で作ってきたが、**Mac 版も作る**。同じ writer / calc の移植なので
「新しいバイナリは作らない」の掟には触れない。ソースで裏を取った現状:

- **自作コード(engine / ooxml / sheet / lang / paper / ui / writer / calc)に
  OS の分岐は一つも無い**(`cfg(target_os)` 不使用)。素の Rust
- フォント探索(`engine/src/font.rs`)は最初から `/Library/Fonts`・
  `/System/Library/Fonts`・`~/Library/Fonts` を見る。ttc も読めるので
  ヒラギノはそのまま拾える見込み
- GPUI は Zed の母国が Mac(Metal)。GPU 描画・IME はむしろ本場。
  rfd のダイアログもネイティブに出る。排他ロック(`.~lock.名前#`)は
  ただのファイル操作でそのまま動く。`.linklibs/` の回避策
  (libxkbcommon-x11)は Linux 固有で、Mac では不要
- **唯一の本物の穴は檻。** bubblewrap は Linux 専用で、calc は
  `/usr/bin/bwrap` を直に見ている。いまのまま Mac で組むと、`@名前`
  (ブック搭載コード)と `=PY`/`@計算` は「檻がありません」で**正しく
  拒否される**(未移植でも嘘はつかず、安全側に落ちる)。一行コードと
  .py 選択実行は動く(深層防御が効かないだけ)

方針: Mac の檻は Seatbelt(`sandbox-exec`)で**同じ4条件**を組む —
網の遮断・実ファイルは読み取り専用・ホームは不可視・書けるのは交換用の
一時領域だけ。`@名前 net` の段階制(許可は毎回の明示の操作、ブックには
保存されない)もそのまま。実装は檻の組み立てを1箇所に集め、bwrap と
sandbox-exec を裏で差し替える形にして、4条件は両 OS 共通のテストで
突き合わせる。sandbox-exec は Apple が非推奨と言い続けているが現行
macOS で動く — 使えなくなったらそのときに考える。**檻が組めない環境では
今までどおり実行を拒否する**(黙って檻なしに落とすことはしない)。

## writer の HTML — JS なしの閲覧と記入(2026-08-04 発注者)

Web の原型(文書+フォーム+サーバー側ロジック)を writer で受ける。
狙いは公開 Web のブラウザ代替ではなく、**閉域・社内の業務 Web と
自作サーバーの「閲覧と記入」側**。実装は writer 側(相方)。

- **開く**: .html(ローカル)と、サーバーが返す HTML。構造(見出し・段落・
  リスト・表・画像・フォーム)+ごく基本の CSS を kumihan の紙面に写す。
  目標は「文書として読める・刷れる」で、ブラウザの再現ではない。
  理解しない要素は帳簿へ(黙って落とさない)
- **JS は実行しない(読み飛ばす)。** 「開く=実行」を持たない家訓の適用。
  JS 前提のページは動かない — 読めないものは読めないと言う
- **記入**: フォーム(input / select / textarea)を記入欄として見せ、
  送信は GET / POST の往復。表計算の入力規則と同じで、書いて・送って・
  返ってきた文書を読む、の繰り返し
- **コードは文書と一緒に旅をしない**(発注者確定)。サーバーがコードを
  配って手元で実行させる形(Web の JS の形)は作らない。サーバー側の
  ロジックは運用者が Python で別途書き・別途配る。手元の補助が要る場合も
  Python in Calc と同じ別経路(明示の操作+檻)— **文書の配布路と
  コードの配布路を分けたまま保つ**
- **網の線引き**: 通信は「いま開いているサーバーとの往復」だけ。ページ内の
  別ホストの画像・CSS は取りに行かない。ローカルの .html は相対パスのみ
- **保存は docx、印刷は PDF**(HTML 書き出しは作らない — 互換は書式の
  境界で守る)。開いた HTML を docx で保存すれば HTML→Word の変換にもなる
- **ブラウザ化の坂には登らない**: タブ・履歴・キャッシュ・拡張は作らない。
  Cookie・認証は最小限をどうするか、実装の相談事項として残す
- 実装の見立て(2026-08-04 発注者「それほど難しくない」— 同意):
  HTML→モデルの写像はほぼ1対1で受け皿が既にあり、**モデルに乗れば
  画面・PDF・docx 保存はタダで付いてくる**。対象が閉域の業務 Web なので
  パーサも**部分集合の自作で足りる見込み**(html5ever は「公開 Web を
  何でも読む」と約束する場合だけ)。段階と注意点は HIKITSUGI の申し送りに

### Web を作る側も簡単になる(2026-08-04 発注者)

「JS なし・form まで」という線は、読む側(writer)だけでなく**作る側も
軽くする**。JS を捨てると Web 開発の重さの根源 — ビルド鎖・フレームワーク・
クライアントとの状態同期 — が丸ごと消え、残るのは「HTML を組んで返す・
form を受ける」だけ。これは Python の標準ライブラリで足りる仕事で、
実測もある: sample/catalog_server.py は**1ファイル・約170行・依存ゼロ**で、
カタログ配信(HTML と CSV の1ソース2形態)+注文フォーム+受付+台帳まで持つ。

これで「業務ソフトはもう作らなくていい」(端末のソフトの土台の節)の
第3の形が揃う:

1. **ブック**(xlsx = 帳票+檻つきコード+運用)— 共有フォルダで配る
2. **文書**(docx = 様式・カタログ。python-docx で生成)— 同上
3. **閉域の小さな Web**(JS なし HTML + form + Python サーバー)—
   複数人が同時に書き込む台帳・受付がここに収まる(排他ロックの xlsx では
   足りない、同時受付の仕事)

どれも**配布物は文書、コードの配布路は別**(サーバーは運用者の持ち物、
ブックのコードは檻の中)。手元に実行コードが配られる形が無いので、
クライアント側の攻撃面がそもそも生まれない(サーバー側は html.escape を
守るだけ)。**JS が欲しくなったら、それは「文書でない Web」の合図** —
このソフトの範囲の外(「バイナリを増やしたくなったら範囲の外」と同型)。

**公開用の「動き」も2層で作れる(2026-08-04 発注者)。**
①照会→応答の動き(検索・絞り込み・ページ送り・受付)は**サーバー側の
Python がページを組み直す** — form の GET/POST の往復で、JS は要らない。
②手元の見た目の動き(分類の開閉・行の強調・畳み)は **HTML/CSS の宣言**
(details/summary・:hover・CSS アニメーション・フォームの検証属性)—
どれも「実行コード」ではないので家訓に触れない。実測は
sample/catalog_server.py の catalog.html(検索=①、分類の開閉=②)。
②の宣言的な部品を writer がどこまで読むかは実装時の判断
(details は「開いた文書」として読めれば足りる)。

**商品は絵で選ぶ(2026-08-04 発注者)。** form は受付の底であって、
カタログの本業は**選ばせる**こと — 商品が見えて、どれがいいか分かること。
これも実行コードなしで作れる(見本 catalog.html で実測):

- **絵**: サーバーが商品ごとの SVG を配る(生成も標準ライブラリで足りる —
  文字だけの SVG は組み立てるだけ。写真ならファイルを置いて配るだけ)。
  陳列は CSS grid のカード(絵・品名・値段が一目)
- **選ぶ**: カード全体が label = 押すと札が入る。選んだ札が光るのは
  CSS の `:has(:checked)` — ここまで宣言だけ
- **比べる**: 選んだ札を「比べる」で送ると、**サーバーの Python が
  比較表を組み、いちばん安い列に印を付けて返す** — 「どれがいいか」の
  判断材料を出すのはサーバー側の仕事にできる
- **writer への含意**: HTML 読みで **img は後回しにできない**(カタログは
  絵が本体。文字が読めても絵が出ないカタログでは選べない)。段階①の
  部分集合に img(同一サーバー / ローカル相対のみ)を含めること

**改訂(2026-08-04 発注者確定): Pyodide / PyScript はやる。**
①②だけでは「押していないのに画面が動く」類が作れず、**受け入れられない**
— 採用されなければ、この安全設計は誰のことも守れない。当初「ブラウザで
Python を動かす形はやらない」と書いたが、同日に改めた。筋はこう通す:

- **ページの動きも Python で書く**(JS は書かない。PyScript / Pyodide の
  ランタイムは既製品として使う — 自分たちが JS を書くのは今後も無い)。
  ブラウザでの実行環境は **WASM の砂箱** — Web が既に受け入れている檻
- **writer で開いたときは自動実行しない。** ページに載った Python は
  Python in Calc のブック搭載コードと**同じ扱い** — 明示の操作+
  bubblewrap の檻(網なし既定・`net` は都度明示)。「開く=実行を持たない」
  は writer では不変。実行しなくても文書として読める作りを求める
  (動きは飾り、中身は文書 — 劣化は正直に)
- **線引きの言い直し**: 「配ったコードは実行しない」ではなく
  **「配ったコードは砂箱の外で実行しない」**。ブック(Python in Calc)が
  既にこの形で運用されている — HTML の Python も同じ線に揃えるだけで、
  家訓の核(裸の実行経路を持たない)は動かない
- 閉域は CDN に出られない — PyScript / Pyodide の**ランタイムはサーバーが
  自前で配る**(同梱。Pyodide は十数MB)。公開用は CDN でもよい
- **どこまでのブラウザで動くか(2026-08-04 に確認)**: 条件は
  WebAssembly + JS 有効。ここ数年の Chrome / Edge / Firefox / Safari なら
  全部動くが、「すべて」ではない — IE・古い WebView・JS を切った環境では
  動かない。だから **form の素の往復が全ブラウザの底**(動きは上乗せ。
  PyScript が動かなくても記入と送信は完結する作りを求める — writer の
  「実行しなくても文書として読める」と同じ規律)。PyScript には
  **MicroPython ランタイム(約170KB・即起動)**もあり、numpy 等が要らない
  動きはこちらが向く(閉域の同梱も軽い)。Pyodide のスレッド系だけ
  COOP/COEP ヘッダが要る — 使わなければ不要

**補正(2026-08-04 発注者): PyScript にはこだわらない。**
約束するのは「**ページに Python を載せてよい・砂箱の外で実行しない**」の
線だけで、PyScript は実行環境の一つに降ろす:

- **writer はネイティブで回す。** ページの `<script type="py">` は、
  writer では WASM もランタイムも経由せず、**機械の Python + bubblewrap の檻**
  で直接回す(Python in Calc の run_python_inner がそのまま実行部。
  明示の操作・網なし既定は不変)。PyScript の API 互換は目標にしない —
  ページに要るのは**記入欄の値の出入り**だけ: calc の `b`/`s` に相当する
  束縛として `form`(記入欄の名前→値の辞書)を渡し、返った値を writer が
  紙面に書き戻す。DOM は持ち込まない
- **閉域は writer が動きの器。** ランタイム同梱(十数MB)の話は閉域からは
  消える — サーバーは HTML を配るだけでよい
- **ブラウザ向けは任意の上乗せ。** 公開用で手元の動きが欲しければ
  PyScript / MicroPython を足せる(その時も form の往復が底)。
  約束はしない — 「全ブラウザの底は form」の規律が正

## 里程標

- **K0 済** 組版の核(`engine`/crate kumihan): 禁則・欧文語の不分割を実フォント字幅で。
  テスト6件 + A4 PDF で目視検証。**禁則は行を折る瞬間に解決**する
  (後処理で字を送ると行長を超える。一度踏んで直した)
- **K1 済** GPUの窓(`viewer`): GPUI(vendor/zed, Apache-2.0)採用可否を検証し採用。
  実Vulkan/waylandで安定起動。**gpui_platform に features=["wayland","x11"] が要る**。
  この機械は libxkbcommon-x11 の開発リンクが無く sudo不可 → `.linklibs/` + `.cargo/config.toml` で回避。
  画面キャプチャはGNOMEが拒否するため自動取得不可(窓は実機で目視確認済み)
- **K2 済** 編集(`engine/src/edit.rs`): カーソル・選択・undo/redo・**IME**。テスト11件。
  **IMEの肝**: 未確定(marked)は undo の単位にしない。確定時の戻り先は
  「変換が始まる前」— 未確定を消した後を控えると、選択を置き換える変換で壊れる(踏んで直した)。
  位置はバイトで持ち、GPUIのUTF-16 APIとは境界で変換
- **K3 済** docx 読み書き(`ooxml`): zip + quick-xml で自作(既存crateを継がない)。
  段落・ラン・文字サイズ・改行・**表(w:tbl)**。テスト10件。
  **実物8件(県・機構の様式)すべてで読み書き成功**し、書き出しを LibreOffice が開けることを確認。
  表は日本の事務様式の本体(実物8件すべてに w:tbl)なのでK5から前倒しした。
  未対応は黙って落とさず `Report` で報告する(画像・入れ子の表)。
  **セル結合(gridSpan/vMerge)は読み・組み・返せる**(2026-08-03。
  結合は様式の見出しの作り方なので、組版では罫線を格子ではなく
  **結合後のセルの縁**に引く)
- **W 済** `writer`(docx互換のワープロ、57MB): GPUIの窓 + kumihanの組版 + ooxml。
  実物の様式docxを開いて紙面に組み、保存できる。読み飛ばしたものは画面に帳簿で出す
- **C0 済** `sheet`(xlsx の核): 読み書き + 式の再計算。テスト13件。
  **日本語特有のバグを実物で発見・修正**: sharedStrings のふりがな(`<rPh>`)を
  本文に混ぜていた(「提案見積書テイアンミツモリショ」)。欧米実装が落としがちな箇所。
  循環参照はエラーの伝播で検出(検出後に上書きされるバグを踏んで直した)。
  絶対参照 `$C$5` も直した
- **C1 済** `calc`(xlsx互換の表計算、48MB): GPUIの格子・数式バー・再計算・保存。
  実物の提案見積書(67セル/式16)を開いて動作。writer と同時に別プロセスで動く。
  **複数シート**(2026-08-03): 画面下の耳で切り替え・追加。カーソル・窓・固定は
  シートごとに覚える。undo の控えは**どのシートのものかを持つ** —
  切り替えた後の undo が別のシートへ他所の中身を書き戻さない。
  別のシートの操作を戻したときは、そのシートへ移って見せる
- **E 済 編集の結線 — これが存在理由**(2026-08-02)。`ui` crate に入力の道を1本化し、
  writer と calc の両方が同じ挙動になる。GPUI の `EntityInputHandler` に委譲:
  - **IME が通る**。未確定(marked)は下線つきで本文に見え、確定して初めて undo の1手
  - キー割り当て: 矢印/Shift+矢印/BackSpace/Delete/Ctrl+A/Enter/Ctrl+Z/Ctrl+S/Ctrl+O/
    **Ctrl+C・X・V**(calc は上下/Tab でセル移動、移動の前に必ず確定する)
  - マウスの範囲選択(2026-08-03): calc はドラッグと Shift+クリックで範囲を選べる。
    押した位置が始点、離したところで確定(セルの外で離しても引きずらない)
  - 窓の操作(2026-08-03): GNOME の Wayland はサーバ側の飾り(タイトルバー)を
    付けてくれないので**自前で持つ** — リボンの空いた帯が取っ手(掴んで移動・
    二度押しで最大化)、右端に ─ ▢ ✕。終了は ✕ / Ctrl+Q / WM の閉じる要求の
    どれでも同じ道を通り、**書きかけがあれば保存するかを確かめる**
    (「はい」でも保存が済まなければ終了しない — 黙って捨てない)
  - 右クリックのメニュー(2026-08-03、calc): 右クリック / メニューキー /
    Shift+F10 で出る。**並びと名前は Euro-Office の右クリックメニューに合わせる**
    (編集で一番使う場所、はユーザー確認済み)。**灰色は全部埋めた**(2026-08-03):
    - 切り取り/コピー/貼り付け、挿入▸・削除▸(セルのシフト4方向+行・列全体。
      シフトは動いたセルへの参照も直し、結合をまたぐときは断る)
    - 消去▸(すべて/テキスト/書式)、並べ替え▸(昇順・降順)、フィルター▸・再適用
    - コメント(commentsN.xml+最小VMLと往復。赤い角印+カーソルで本文表示)
    - セルをフォーマットする(モーダルにしない書式の道具箱: 罫線・塗り・文字色・
      揃え・折り返し・表示形式)、数値の書式▸
    - 条件付き書式▸(cellIs を持ち・効かせ・往復。他の型は報告して落とす)
    - ドロップダウンリストから選択(同じ列の値が候補。入力規則が無くても効く)
    - 名前の定義(=単価*2 が書ける。definedNames と往復し、Print_Area 等の
      理解できない definedName は原文で持ち越す)
    - 関数を挿入▸、ハイパーリンク(rels と往復。青字・Ctrl+クリックで開く)、枠の固定
    - **形式を選択して貼り付け**(本家に無いのが残念、との声で足した唯一の独自項目。
      2026-08-03): 値だけ(Ctrl+Shift+V)/ 式をそのまま(ずらさない)/ 書式だけ
      (アプリ内のコピー限定)/ 転置(値を貼る — 式の参照を転置で回す嘘はしない)。
      外から来た `=` 始まりの欄は、値貼りでは文字として置く(黙って式にしない)
    AI は入れない(方針)。文字入力が要るもの(名前・コメント・リンク・条件の値)は
    writer の検索欄と同じ「板」方式 — 開いている間は打鍵がそこへ、Enter 決定・Esc 取消。
    選択の中で右クリックすれば選択への操作、外ならそのセルへ移ってから。
    Esc は板→一覧→子→親→道具箱の順に閉じる
  - スクロール(2026-08-03): writer はホイールで紙を送り、**打つとキャレットの
    位置まで紙が追従する**。紙の頭より上・末尾より下へは行かない。
    IME の候補窓もスクロールと倍率を織り込んだ位置に出る。
    calc はホイールで窓(view)を動かす — 触板の細かい送りは端数を貯めて捨てない
  - コピー&ペースト(2026-08-03): writer は本文の文字。calc は範囲を **TSV** で
    系のクリップボードへ(他のアプリと相互に貼れる)。アプリ内の貼り付けは
    式の相対参照を貼り付け先へずらし、外から来た TSV はずらさない。
    **貼り付けても書式は据え置き**(帳票の枠を壊さない — 範囲 Delete と同じ規則)。
    切り取りは移動なので参照をずらさない
  - **踏んだ点**: 入力ハンドラは `paint` のときに `window.handle_input` で差す。
    描画ツリーに何も描かない `InputSink` 要素を置く。これが無いと IME もキーも届かない
  - **本文を編集しても表は消えない**(`Document::set_body_text` が表を残す)。テストで固定
  - 検査: `ui` 10件・`sheet` 16件。実物の様式docx/xlsxを開いて打って保存し、読み直せる
- ~~K4 縦書き・ルビ・均等割付~~ — **後回し**(2026-08-02 ユーザー判断)。
  存在理由は「日本語組版の見せ場」ではなく「**実際に使える**こと」。
- **K5(進行中)** 表の**組版は済**(列幅 gridCol・セル内の折り返し・
  セル結合まで。画面と PDF は同じ紙面を写すので両方に出る)。
  残り: 様式・印刷(yoshiki 合流)
