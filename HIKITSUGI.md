# 引き継ぎ(2026-08-03)

別の環境で作業を引き継ぐ人(または Fable)への申し送り。
**設計と決定事項の正本は [SEKKEI.md](SEKKEI.md)** — まずそちらを読むこと。
ここには SEKKEI に書かない「進め方の作法」と「直近の状態」だけを書く。

## 進め方の作法(発注者と確立済み)

- 発注者は実機で GUI を触り、日本語で不具合を報告する。
  **1報告 = 原因究明 → 修正 → `cargo test --workspace` 緑 →
  `cargo build --release` → コミット**、の回転。発注者は
  `./target/release/{writer,calc}` を直接起動して確かめるので、
  **release の組み直しを忘れると「直っていない」と見える**
- 原因は推測で直さず、`vendor/zed/crates/gpui` のソースで裏を取る
- コミットは日本語で「何を・なぜ」。設計判断と踏んだ穴は SEKKEI.md に追記
- 家訓: できないものを、できるように見せない(未実装は灰色。ready の嘘は
  wiring_tests が落とす)/ 黙って落とさない(Report へ)/
  書式は据え置き / どの操作も1手で戻せる

## 環境の注意

- cargo が PATH に無い機械なら `export PATH="$HOME/.cargo/bin:$PATH"`。
  そもそも rustup ごと無い機械もある — その場合は user レベルの導入で足りる
  (`curl https://sh.rustup.rs | sh -s -- -y`。sudo 不要。2026-08-03 に実施済み)
- `.cargo/config.toml` と `.linklibs/` は**この機械固有**(libxkbcommon-x11 の
  開発リンク回避)。別の機械では不要か、作り直しが要る。
  作り直しは2手: `.linklibs/libxkbcommon-x11.so` → 実行時の `.so.0` へ symlink、
  `.cargo/config.toml` に `rustflags = ["-L", "<絶対パス>/.linklibs"]`
- 実物の様式(検査の材料)は
  `/mnt/sdb/home/dev/ドキュメント/機構/yoryou-yoshiki/` にある。
  **無い環境ではテストは黙って飛ぶ**(失敗はしない)。可能ならコピーして持つ
- Python 検証は miniforge の `.venv`(polars / python-docx)。無ければ
  `conda create -p .venv python polars python-docx`

## GPUI の踏み跡(再発させない)

- div の既定レイアウトは **Block(縦積み)**。入力とマウスの受け皿
  (InputSink)は absolute + inset 0 で全面に重ねる
- マウスの bubble 配送は**後に登録した方が先**。重ねるメニュー類は
  InputSink より後に描き、項目側の on_mouse_down で `cx.stop_propagation()`
- mouse-up は**ポインタが乗っている要素にしか来ない**。ドラッグ終了は
  `window.on_mouse_event` + move 時の `pressed_button` 確認で自癒させる
- **click_count で「押す系ジェスチャ」を分岐してはいけない**。Wayland は
  400ms 以内・近距離の押し直しを 2,3,… と数える(位置は押下同士の比較)ので、
  やり直しのドラッグがダブルクリックに化ける。calc の列幅境界の
  「ダブルクリックで自動調整」はこれで発注者のドラッグを横取りしていて撤去した
  (2026-08-03。入れ子 mutter + xdotool の自動再現で特定。測る仕組みは
  git 12e71bf にある — 復活させるならメニュー等の押下と衝突しない場所へ)
- 文字の行の高さは既定で黄金比 → グリフは div の頭から約 0.309×サイズ下に
  描かれる(writer の `HALF_LEADING`)。自前で引く線・帯はこれを織り込む
- rfd のダイアログは同期 = 主の糸を塞ぐ。終了確認・ファイル選択は
  `background_executor().spawn` + `cx.spawn` で別の糸に出す。
  **calc・writer とも全ダイアログ(開く・保存・PDF・画像)を別糸化済み**
  (2026-08-03)。新しいダイアログを足すときも同じ作法で
- この作業木で **git sparse-checkout が突然有効になっていた**事故があった
  (2026-08-04。engine/ が作業木から消えた。`git sparse-checkout disable` で
  復旧)。誰の操作か特定できず — 見かけたら同じ復旧を。stash/checkout 同様、
  共有作業木では sparse-checkout も使わないこと
- **vendor/web-apps は元の機械の版でないと再生成できない**(2026-08-04 に
  ONLYOFFICE master を取ってみたらテンプレート構成が変わっていてタブが欠けた。
  この機械では ribbon.rs への手反映+gen_ribbon.py の同期、の二重書きで運用)
- リボンは生成物: `python3 ui/gen_ribbon.py ja > ui/src/ribbon.rs`。
  押せるものは同スクリプトの READY 表。テンプレートの class 注入スロット
  (pagebreak・insertimage 等)は id の正規表現に掛からないので手で差し込む

## 実装の注意

- 保存は `write_with(原本)` が基本(xlsx / docx とも)。原本を渡さないと
  図形・スタイル等の部品が消える
- `kumihan::Paragraph` に性質を足すと、全指定 literal が数カ所で割れる
  (engine/examples/page.rs、ooxml のテスト)。編集の持ち越しは
  **run 粒度の splice**(SEKKEI「writer の編集モデル」)— 段落の性質も
  run の境も、編集で流されない
- calc の編集判定は `editing()`(数式バーとセルの保存内容の差)。
  「バーが空か」で分岐してはいけない(バーには常にセルの中身が写る)

## 直近の状態(詳細は git log)

済: calc の右クリックメニュー(Euro-Office 準拠・灰色ゼロ)、
Paste Special(独自)、複数シート、条件付き書式・コメント・リンク・
名前の定義(xlsx 往復つき)、コピー範囲の破線、
列幅・行高のドラッグ(見出しの境界を掴む。undo 可・xlsx 往復は元からある)、
列・行の選択(見出しをクリック/撫でる/Shift+クリック。左上の角で全選択。
見出しの右クリックは選んでからメニュー。選択中の見出しは色が変わる)、
グラフの挿入(裏方 matplotlib。SEKKEI「calc の分業」の改訂を参照)と
画像の挿入・表示(浮かぶ絵。読んだ xlsx の絵も出す。保存は writer と同じ
images/images_new の二本立て — 原本の drawing には**錨を継ぎ足す**)、
検索と置換(板2枚。空 Enter=次を検索)、書体と大きさの一覧・大文字小文字、
関数の拡充(VLOOKUP/INDEX/MATCH・TODAY/DATE 系・PMT 系。数式タブ灰色ゼロ)、
印刷の仕上げ(拡大縮小・改ページ・タイトル行の繰り返し・枠線/見出しの印刷。
printOptions は今まで持ち越しから漏れていた — 直した)、
図形の挿入(SVG 方式 — SEKKEI「calc の分業」参照。六種、選択・移動・
右下で大きさ変更・Del 削除。xlsx へは prstGeom、PDF は輪郭のみ)、
テキストボックス(図形+文字。文字は GPUI の重ね描き=日本語の組みが正、
xlsx へは txBody 往復。図形を選んで Enter でいつでも書き直し)、
スパークライン(選択の数を折れ線に。**その時の値で描く固定の線**と
status で言う。xlsx へは custGeom の折れ線 = Excel でも見える)、
排他ロック(.~lock.名前#。ファイルサーバーの「後勝ちで潰す」を防ぐ。
先客が居れば名乗りを見せて上書き保存を止める。**writer にも同じものが要る** —
lock_path_for / foreign_lock / lock_identity は calc/src/main.rs にある。共通化歓迎)、
リボンの全部入れ(発注者確定: コラボレーション・保護・プラグインのタブを
両アプリに追加。場所は本家どおり・当面灰色。calc のコメントを追加だけ配線済み。
writer 側の コメント/変更履歴 の釦は相方の実装に繋げられるはず — 申し送り。
2026-08-04 第2弾で本家の残りタブも追加し**全タブ完了**: calc に
ピボットテーブル・表のデザイン(データの直後)、writer に フォーム
(参考資料の直後)。表記は本家 ja.json、位置指定は gen_ribbon.py の
APP_TABS。表のデザインの「重複データを削除」だけ既存の rem-duplicates に
配線、他は灰色。タブの有無は試験「本家のタブが全部ある」で固定)、
ピボットテーブルの挿入(発注者確定: polars 裏方で挿入だけ動かす。
板3枚で 行 → 列(省略可)→ 値と集計 を聞き、結果はその時の値で元の表の
右の空きに置く。新しいシートは undo で消せないのでシートには足さない。
台本は PIVOT_PY、指図は JSON、答えは CSV 取り込みと同じ区切りの印。
合計の無い組み合わせは polars の流儀で 0 になる — 試験がそう言っている。
2026-08-04 続き: タブの残り全部 — 指図 PivotDef を xl/joPivot.xml で往復
(joPython と同じ独自部品方式)、更新=同じ場所に polars で置き直し
(広がって塞がる時は断る)、総計/小計/空行/レイアウトは指図の旗+置き直し。
台本の答えは行ごとに種別つき(h/d/s/b/t)で、小計・総計は太字になる。
総計の「1行に集める」は stub の小技=見出し列を札で潰してピボットに通す
と列名が揃う。試験は台本の実走+指図の往復。ピボットタブ灰色ゼロ)、
グループ化(データタブの4釦。モデルは Sheet の row/col_outline(深さ)と
row/col_hidden(畳み)、xlsx の outlineLevel/hidden と往復 — 中身の無い行の
<row> も捨てない。cols は幅・深さ・畳みを1走査でまとめて出す。画面は
visible_rows/visible_cols の一点で飛ばし(当たり判定も同じ道)、紙(PDF)も
幅・高さゼロで飛ばす。詳細の表示/非表示は選択、無ければカーソルの行の
ひとつながり。アウトラインの +/-(gutter)も済(2026-08-04):
かたまりが**終わった直後の行**の見出し左に丸釦を描き、− で畳み + で開く
(見出しの重ね描き。列の gutter は無し — 詳細の表示/非表示で足りる)。
発注者指摘「畳んだとき合計が残らないとだめ」→ **小計**の釦を追加
(Excel の データ > 小計。本家のデータタブには無いが EXTRA_CMDS で置く):
区切りの見出し → 合計の見出し(空=数の列全部)を聞き、apply_subtotals が
区切りごとに =SUM の小計行+総計行を挿し、**明細だけ**グループ化する。
枠の挿入は下から(上の座標が狂わない)、式は最終座標で書く。
insert/remove_row/col が row/col_outline・hidden を運ぶよう直した
(置き去りだと別の行が畳まれる)。試験あり。
※入れ子 mutter の自動操作の罠: 打鍵が届かないときは X の入力フォーカスを
明示的に当てる(python-xlib: set_input_focus。xdotool 無しでも
XTEST + xwd で回せる。Shift+クリックの修飾も同根で落ちる))、
calc の共同編集・プラグインの7釦(writer の仕組みの移植 2026-08-04:
共同編集モード=錠の顔 / コメント削除・表示切替 / チャット=.chat.txt /
バージョン履歴=.jo-history 9世代・一覧は pick 流用・開くと名無しの複製 /
マクロ=run_python_file_dialog と同実体 / プラグイン管理=
~/.config/office/plugins の一覧実行。保存前の keep_version は save_to に。
同日、保護タブ3釦も完了: 保護=xlsx の sheetProtection(シート単位。
堰は commit / fmt / 貼り付け / Del / run_cmd の白表 PROTECTED_OK)、
暗号化=ooxml::crypt を保存/開くに結線(原本持ち越しは original_plain で
平文に解いてから)、署名=writer と同じ .sig / sign.key。パスワードの板は
伏せ字。calc のコラボ・保護・プラグイン灰色ゼロ。
続けて**画面の額縁も calc へ写した**: 1段目=QA+ブック名(緑帯・取っ手)、
2段目=白地タブ+緑下線+🔍、下端=シートの耳+状態+選択の合計/平均/個数
(sel_stats。200,000 セル超は数えない)。保護シートの耳に 🔒)、
表のデザインの実装(表オブジェクトは持たない — 選択に1手ずつ掛ける道具:
ヘッダー行の帯・縞模様の行/列・最初/最後の列の太字・合計行=下に =SUM の
行を足す(下に中身があれば断る)・フィルタのボタン=setfilter と同実体。
**方針変更 2026-08-04(発注者「表オブジェクトを持たない設計は方針変更」)
→ TableDef を持つことにした**: xl/tables/tableN.xml + tableParts + rels +
Content_Types と往復。範囲に変換=表を外す(書式は残す)、サイズ変更=板で
範囲を打ち替え。表を減らしたら部品も CT の宣言も消す(残すと Excel が
壊れたと言う)。表のデザインの各釦は、表の中なら表の性質も一緒に更新。
同時に**右横書き**も実装(発注者「日本語は、たまに右から書く場合がある」)—
Sheet.rtl ↔ sheetView rightToLeft は visible_cols() を逆順にする一点だけ
(描画も当たり判定も同じ関数を通るのでずれない)、CellFormat.rtl_text ↔
readingOrder="2" はセルの中を1字ずつ右から。ラテン文字の bidi は扱わない)、
PY の UDF とスピル(=PY("関数",…) セル。@計算 のときだけ檻で評価、
開く=実行なし。2次元はスピルし衝突は #SPILL!。前史は発注者の 2018 Qiita)、
**Python in Calc**(データ > Python。板に一行 / .py 実行 / @save でブックに
コードを載せて持ち運ぶ。載ったコードは**必ず bubblewrap の檻**で実行、
開いても自動実行しない。設計は SEKKEI 参照。office_sheet.so を calc の隣に)、
データタブの Python 裏方ほか(テキストからデータ=CP932 も判定する CSV 読み、
区切り位置、ゴールシーク=割線法を表の複製で回す、外部リンク=値で取り込む。
ソルバーも実装(発注者確定 2026-08-04: 入力画面は ONLYOFFICE の
「ソルバーのパラメータ」と同じ形の小窓 — 目的/最大・最小・値/変数セル/
制約の追加・変更・削除/非負/単体法 LP。多欄の小窓は solver.focus で
打鍵の宛先を切り替える(HasEditor が見る)。係数は表の複製から測り
(全0→単位ベクトル→全1で線形の検算)、線形でなければ正直に断る。
解くのは SOLVER_PY = scipy.optimize.linprog(highs)。scipy は .venv に
pip で追加済み。セル参照は小文字も $ も受ける)、
SmartArt(発注者提案「ONLYOFFICE からデータを取得」: 分類・並び・名前は
web-apps の define.js + ja.json の現物、SMARTART 表に採録。挿入は図形の
集まり(9形: カード型/縦方向/ピラミッドのリスト・基本ステップ/プロセス/
タイムライン・基本の循環/ボックス循環・組織図/階層・ベン図・マトリックス・
ピラミッド)。**SheetShape に dx_px/dy_px を新設**(錨セルからのずらし。
oneCellAnchor の colOff/rowOff へ書き、画面・PDF・当たり判定・ドラッグが
織り込む — ドラッグは px 精度になった。読みも対応済み(2026-08-04:
colOff/rowOff を読んで dx/dy に入れる。往復試験あり)、
方程式(発注者「writer の仕組みを移せば」: writer の方式=図は Python で
描いて画像で貼る、を calc 流に自動化 — 板に TeX(mathtext)を打つと
EQ_PY が清書して透過 PNG をカーソルへ。読めない式は canvas.draw で
止まって status に出る。試験は実 matplotlib で PNG の中身まで)、
スライサー(発注者「簡単では」: カーソルの列の一意な値を釦で並べる小窓。
押して絞る・≡=複数選択・✕=解除・Esc で閉じる。絞りは visible_rows の
slicer_keeps で、絞り込みと同じ**見え方だけ** — xlsx のスライサー部品は
書かない。値は64個まで表示)、
チェックボックス(セルの部品: Bool のセルを ☑/☐ で描き(カーソル上は
編集の真実 = TRUE/FALSE のまま)、**空白キーで切替**(replace_text_in_range
の入口で、打ちかけ・板・小窓が無いときだけ空白を奪う。保護も堰く)。
挿入釦は空のセルに FALSE を置くだけ — 中身のあるセルは潰さない。
xlsx へは普通の TRUE/FALSE(cellControls の部品は書かない=正直な劣化)。
紙(PDF)も対応済み — ただし ☑/☐ の字は Noto CJK に**無い**(実測)ので
文字ではなく線で描く(箱+チェックの折れ線)。豆腐を刷らない)、
テキストアート(方程式と同じ道 = insert_py_image に共通化。TEXTART_PY が
太字+縁取り(calc の緑)の飾り文字を透過 PNG に描く。**挿入タブ灰色ゼロ**)、
calc のファイル選択の別糸化(開く・保存・PDF。保存が済んだときだけ終了)、
PDF の塗り・文字色・条件付き書式(切れた列の数も返して status に出す)、
データの入力規則(list を読み・効かせ・往復・リボンから作成/解除。
設計は SEKKEI「calc に残すもの」。条件付き書式と名前の管理もリボンから)、
PDF が帳票の印刷設定に従う(pageSetup の向き・用紙(B は JIS)・
pageMargins・Print_Area。効かせたものは status に言う)、
レイアウトタブの印刷設定(向き・用紙・余白・印刷範囲を設定できる。
Print_Area はモデルに昇格し undo 可。保存は原文の pageSetup に
**属性だけ差し替え** — 拡大縮小など知らない属性は残す)。
writer の選択描画・ドラッグ選択・全カーソル操作・右クリックメニュー・
段落の帯と囲み枠・画像の挿入(media/rels/Content_Types 生成)・
ヘッダー・フッター(板で編集・ページ番号は PAGE フィールド往復・
PDF は各頁の番号。設計は SEKKEI「writer のヘッダー・フッター」)・
見出しスタイルと目次(ホーム > 段落のスタイル → 参考資料 > 目次。
設計は SEKKEI「writer の見出しと目次」)・全ダイアログの別糸化・
複数レベルのリスト(印はレベルで変わる。Tab/Shift+Tab で深さ)・
ページ数と日付(ヘッダー/フッタータブ灰色ゼロ)・ダークモード(紙は白のまま。
表示タブ灰色ゼロ)・行番号・ファイルからのテキスト・テキストの追加・
目次の番号の右揃え(字幅で点線を詰める)・キャレットの大きさ追従・
しおりとコメントの錨の保存持ち越し(黙って捨てていた穴を塞いだ)・
挿入系の配線(図形・グラフ等=画像を貼る/テキストボックス=1×1の表。
SVG も受ける — resvg で高精細の PNG に直す。matplotlib は savefig("図.svg"))・
コメント(段落単位。comments.xml 往復)・ページ色(w:background +
displayBackgroundShape)・透かし(ヘッダーの VML と往復。紙は回転した字)・
ブックマーク(名前の往復+一覧・移動・追加・削除の板)・
図表番号(「図 N」の中央揃え段落)・図表目次(Tof の印。目次と同じ差し替え)・
段組み(w:cols 往復。細い行長で組んだ巻物を fold_columns がページの
物理座標へ折る — 座標を使う側は無変更。発注者の指摘どおり難しくなかった)・
描画(ペン・蛍光ペン・消しゴム。設計は SEKKEI「writer の描画」)・
変更履歴(保存時に差分を w:ins/w:del へ。設計は SEKKEI「writer の変更履歴」)・
ドロップキャップ(Word の作法で framePr の枠の段落に割って書き、読みで合流)・
ハイフネーション(Knuth-Liang パターン同梱。settings の autoHyphenation 往復)・
**編集モデルの run 粒度化**(部分書式=選択の字だけ太字などが可能に。
段落の増減で性質がずれる持病・表が末尾へ動く持病も解消。
設計は SEKKEI「writer の編集モデル」)・
相互参照(run が持つ REF/PAGEREF。設計は SEKKEI「writer の相互参照」)・
writer の排他ロック(calc と同じ .~lock.名前#。先客が居れば上書きせず
別名保存へ。終了確認も calc と同じく実ファイルの未保存変更だけ)・
**コラボレーション・保護・プラグインの8釦**(発注者確定 2026-08-04
「灰色のままにしない」: 保護=documentProtection readOnly の往復+
効き目本物 / 共同編集モード=排他ロックの顔 / バージョン履歴=
.jo-history に9世代の控え+板 / チャット=隣の .chat.txt の言伝 /
マクロ=檻の中の python-docx が複製を直す(Ctrl+Z で戻る)/
プラグインの管理=~/.config/office/plugins の一覧実行 /
暗号化=ECMA-376 Standard AES-128 を ooxml::crypt に自作
(msoffcrypto で解ける=形式が本物。Agile はまだ読めないと言う)/
署名=隣の .sig に Ed25519 の添え書き。設計は SEKKEI の
「writer の保護と共同編集」)。
writer は 88/101(残る灰色はフォームタブ10・縦書き・sdt・配色)。
**calc の同じ釦はまだ灰色** — ooxml::crypt(暗号化)や writer の
lock の顔・.chat.txt・.jo-history の作りはそのまま移せる(申し送り)。
calc の表示タブを本家に揃えた(発注者スクショ 2026-08-04: ズーム・
数式バー・見出し・0を表示する を追加。ズームは col_px/row_px/セルの文字を
self.zoom 倍、行高ドラッグは /zoom で割り戻す。見出しは head_w()/head_h()
に集約し 0 にできる — 当たり判定・原点・グリップも同じ関数を通すので
消しても掴めない事故が無い。0 表示は描画で空文字に差し替え=値は不変。
シートの表示・インターフェイステーマは灰色。※実機の押下確認は入れ子
mutter の装飾ぶれで座標が滑り不安定 — タブ描画の目視と wiring_test で担保)。
**残りの灰色を全部つぶした**(発注者「残りを作ってしまいましょう」
2026-08-04): 下付き(vertAlign)・両端揃え(HAlign::Justify を新設)・
文字の向き(CellFormat.rotation。画面は縦積みで見せる — GPUI に字の回転が
無いため。xlsx へは textRotation で往復)・計算方法(auto_calc。手動時は
「計算」で回す)・関数の挿入(分類 → 既存の関数一覧へ)・セルのスタイル
(CELL_STYLES = 名前と書式の組。表オブジェクトは持たない方針どおり普通の
書式を掛けるだけ)・シートの表示(Sheet.hidden を新設。workbook.xml の
state="hidden" と往復。原文へは patch_sheet_states で属性差し替え)・
ウォッチウィンドウ(watch: Vec<(シート, セル)>。値は下の帯に生きて出る)・
描画3種(writer と同じ tool の入切)。**筆も実装済み**(2026-08-04): 1筆を
SheetShape の折れ線(kind="ink" / "marker")として置く — 既にある図形の
仕組みに丸ごと乗るので custGeom で xlsx に入り(Excel でも線)、PDF にも出て、
Ctrl+Z も効く。描きかけは点の粒で見せ、離すと1本の線になる。消しゴムは
ink_at の近傍判定で1筆消す。※罠: 移動の受け口(on_mouse_event の
MouseMoveEvent)は drag 系が立っている時だけ mouse_drag_at を呼んでいた —
筆と消しゴムの条件を足さないと「道具は出ているのに描けない」になる。
**calc 139/145**。残る6つは方針で灰色維持(表オブジェクト2・RTL2・
テーマ色2 = a:theme の読みが要る。writer と同根)。
**テーマ色**(発注者「テーマ色の読みもやりましょう」2026-08-04):
sheet/src/theme.rs を新設 — xl/theme/theme1.xml の clrScheme を読み
(theme1.xml の並びは dk1,lt1,… だが `theme=` の番号は 0=lt1 なので入れ替える)、
`<color theme="4" tint="0.4"/>` を HSL の明度加減で実際の色に解く。
**CellFormat に由来(color_theme / fill_theme)も持つ**ので、配色の変更で
組を入れ替えるとセルの色が追従する(書き出しも theme= のまま返す)。
配色は SCHEMES(Office / 暖色 / 寒色 / 墨)。テーマ部品は持ち越さず
モデルから書き直す(でないと配色の変更が効かない)。
インターフェイステーマ=画面の明暗も実装 — **セルは白のまま**、暗くするのは
帯・タブ・釦・見出し・耳だけ(writer の「紙は白のまま」と同じ考え)。
**これで calc は 145/145、リボンの灰色ゼロ**。
両アプリの窓の移動・終了確認(別糸)・スクロール・クリップボード。
pysheet(Python 束縛、polars 連携)。

## 次の仕事

**両担当へ(発注者 2026-08-04): 設定の器と言語。** 正本は SEKKEI「設定 —
器と言語」。`~/.config/office/settings.toml` + ファイルタブ「詳細設定」
ページ(共有 ui/ に器を作り、両アプリから使う)。環境変数は一時上書きに
降格(優先順: env > toml > 既定)。言語は段階制 — ①器 ②リボンの言葉の
実行時切り替え(gen_ribbon を「ロケール→表」の埋め込みへ。**45言語の
表の生成は vendor が生きている元の機械で**)③状態行・板の文言の lang
への外部化(数百句 — 一番大きい。済むまで「状態行は日本語」と正直に)。
インターフェイステーマの灰色釦はこの器で実体を得る。名乗り(ロック・
チャット・署名の user@host)も設定に。**設定が入ったらマニュアルの
環境変数の記述を差し替える**(マニュアル担当が追従する)。

**calc 担当へ(発注者確定 2026-08-04): AI タブと「VBA の移し替え」第1号。**
設計の正本は SEKKEI「AI メニュー」の節(「AI タブは作らない」の改訂 —
校正はレビューに残したまま、モデルに任せる変換と生成の道具箱を作る)。

- リボンに **AI タブを両アプリに追加**(ribbon.rs + gen_ribbon.py の
  APP_TABS/EXTRA_CMDS 方式で二重書き。writer 側の釦は当面灰色置き)
- **第1号 = VBA の移し替え(calc)**: xlsm の xl/vbaProject.bin から
  コードを取り出し(裏方 oletools — .venv に pip で足す。公共財)、
  モデルが **Python in Calc の台本(b/s 束縛)**へ書き換え、@save と
  同じ形で book.scripts に載せる。**人が確かめてから使う** — 変換結果は
  自動実行しない。元の vbaProject は原文持ち越しのまま消さない
- **宛先の規律**: 既定はローカル(校正と同じ OpenAI 互換の口)。外部
  (Claude API 等)は設定で明示したときだけ・鍵はブックに入らない・
  繋がらなければ「できません」(黙って空にしない)。
  手引きへの反映はマニュアル担当がやる(実装が入ったら教えて)

**済(2026-08-04)**: calc の額縁は第2弾まで写した — 一段の絵釦
(汎用の描き方: タブの並びから自動で組み、BIG 表の釦だけ名札つき大釦。
ホームは2段に割る。乗ると名前が下のステータスバーへ = hover_hint)、
ファイルタブは全面ページ(品書き+ブックの情報。統計は生きた値、
プロパティは docProps/core.xml と**往復** — Book.props。読みは grab、
書きは patch_core_props(持ち越しの core.xml へ差し替え。無い・新規なら
部品と Content_Types と .rels の宣言ごと足す)。編集は板(prop-*)で、
writer と同等になった)。最近開いたは recent-calc.txt 12件。
新規作成 = new_book(未保存があれば断る)。ついでの実バグ修正:
罫線の辺が border_color(全辺1色)で他の辺を巻き込むのを、辺ごとの
重ね描き(1px の帯)に直した(0b94b97 — writer も同じ穴が無いか一見の価値)。

発注者の確定(2026-08-03): **このソフトは事務様式専用ではない(汎用)。
きちんと動作する釦は灰色で残さず普通の釦にする。**
挿入系=画像を貼る、コメント=段落単位、透かし・ページ色は実装済み。

**writer の HTML(発注者確定 2026-08-04)— 閲覧・記入・公開 Web まで済み。**
設計の正本は SEKKEI「writer の HTML — JS なしの閲覧と記入」。

- **できていること**: `kumihan::html`(自作の部分集合パーサ。約400行)で
  .html を文書として開く — 見出し・段落・書式・箇条書き・表・HTML の
  ruby(うちのルビへ)・title(文書の情報)・実体参照。p/li/tr/td の
  閉じ忘れは癒す。**script は丸ごと読み飛ばす(JS は実行しない)**。
  文字コードは UTF-8 → CP932(encoding_rs)。form は記入の板で書いて
  GET/POST(urlencoded)、答えの HTML をまた読む。リンクの板から辿れる。
  http と https の両方(rustls)、転送(3xx)も5回まで追う。
  **網の線引き**: 読みは公開 Web まで広く、**書き(送信)は開いた起点だけ**。
  Cookie は持たない。保存は docx(HTML 書き出しは作らない)
- **見立ては当たった**(発注者「それほど難しくない」— 同意していた):
  HTML→モデルの写像はほぼ1対1(h1〜h3=Heading、p=Body、table=w:tbl、
  ul/ol=箇条書き、b/i/u=CharFormat)で、**モデルに乗れば画面・PDF・
  docx 保存はタダで付いてきた**。html5ever も要らなかった
- **受入試験**: sample/html の11件(閉じ忘れ・CP932・宣言なし CP932・
  js・form を含む)と sample/catalog_server.py(注文書 form 22欄の抽出と
  POST の往復・リンク辿り)で実地確認済み
- **残る仕事(writer 担当へ)**: ①**ページに載った Python を檻で回す**
  (`<script type="py">` 等。発注者確定「Pyodide / PyScript もやる」の
  writer 側の含意。**明示の操作でだけ**動かし、開く=実行は不変。
  実装は calc の run_python_inner 一族がそのまま使える。約束するのは
  「檻で回すこと」と「form 束縛(記入欄の名前→値の辞書を渡し、
  返った値を紙面に書き戻す)」だけ — **PyScript の API 互換は目標にしない**
  (発注者「こだわらない」)。DOM は持ち込まない)
  ②本文中の記入欄の直接編集(いまは板)③ページ内の別ホストの画像・CSS
- **向きの確認(発注者)**: これは ONLYOFFICE Docs とは別の方向。あちらは
  「オフィスを Web の編集器で実装する」、こちらは逆で **Web を文書として
  読む・記入する**。手本は「HTML→docx 変換器」ではなく「文書として組んで
  見せ、form を書けるようにし、POST する」こと(ブラウザ化の坂には登らない)

**K4(日本語一級)の進み(2026-08-04 発注者「やってみて」)**:
均等割付(w:jc distribute 往復・最後の行も配る・画面/PDF とも)と
ルビ(CharFormat.ruby・w:ruby 往復・LibreOffice で実地確認済み。
**w:ruby の親は w:r** — 段落直下だと LO が基底ごと落とす、を踏んで直した)
は実装済み。**縦書きも実装済み**(2026-08-04。fold_vertical が行を
右からの列へ写す。約物は縦用字形へ。ホーム > テキスト方向で入切、
sectPr の tbRl と往復。初版の限界 = 表・段組みは横のまま、ASCII は
縦積み、変換下線なし — SEKKEI「日本語一級(K4)」参照)。
**K4 これで3点とも揃った。**

**writer は灰色ゼロ(114/114。2026-08-04)。** 残っていた
コンテンツコントロール(sdt)+フォームタブ10・配色・複数ページを実装。
設計は SEKKEI「writer の灰色ゼロ」。

**writer の画面(2026-08-04。発注者が画像で指示)**: デスクトップ版の
額縁に合わせた — クイックアクセス行+下線タブ+下のステータスバー、
ホームは2段の絵釦、挿入と残りのタブは一段(名札つきの大釦+絵だけの釦)、
ファイルタブは全面のページ(品書き+文書の情報。プロパティは
docProps/core.xml と往復)。**左パネル**(見出し/コメント/検索の3つの耳。
押すとその場所へ飛ぶ)と**右パネル**(いる場所の文字・段落・ページの設定を
その場で直す盤。釦は run_cmd を呼ぶだけなので実体が二重にならない)は
本物を作った — 最初は既存の板の入切に割り当てただけで、発注者の
「作りましたか」の一言で作り直した。**入切の釦は入っていると枠と色が付く**
(toggled(id)。「押したのに何も起きない」に見えるのを仕組みで防ぐ)。

**メニューの動作試験(2026-08-04)**: `writer/src/main.rs` の
`menu_run_tests` が GUI を起こさずに(gpui の試験の場で)
**リボンの釦を全部おす**。空の文書と sample/writer の5件の両方で回し、
落ちないこと・未配線が無いこと・入切が2度で戻ること・主な釦が
本当に文書を変えること・フォームの釦が記入欄を入れることを見る。
**ファイル選択の窓を開く12個だけは試験から外す** — rfd は画面の無い
試験では返ってこない(踏んで確かめた。実機での確認に回す)。

小さい残件: 変換下線の位置の実機確認、表入りのヘッダー・フッターの編集。
暗号化の Agile 方式(Word/Excel 2013+ の既定)を実装(2026-08-04、発注者
「作れますか」→「やって」): ooxml/src/crypt.rs の後半。SHA-512 の鍵導出を
spinCount 回(10万)+AES-256-CBC の 4096 バイト区分+HMAC-SHA512 の完全性。
base64・HMAC・CBC(ECB の器で連鎖)・XML 属性引きは依存を増やさず自作。
decrypt() は minor==4 を見て agile_decrypt に振る(呼び側は無変更)。
**本物と相互検証**(.venv の msoffcrypto + python-uno の LibreOffice で):
自作の出力を msoffcrypto が解け、LibreOffice が書いた本物を自作が解いた
結果は msoffcrypto と1バイト一致。calc の暗号化保存は encrypt_agile へ
切り替え(AES-256。読みは Standard も Agile も両対応)。
**writer も encrypt_agile に替えた(2026-08-04。申し送りは解消)** —
保存は AES-256、読みは Standard も Agile も両対応なので前に作った
AES-128 の文書もそのまま開ける。自作の出力を msoffcrypto が解けることを
writer 側でも確認済み。
※罠: msoffcrypto の encrypt は壊れた CFB(宣言長と実体長が食い違う)を
書く道具なので、書きの検証には使えない — 読みの参照専用。書きの検証は
LibreOffice(python-uno、自分のプロファイルと別ポートで起動)で。
(「すべて置換」の書式均しは 2026-08-04 に解消済み — 1置換ごとに写す)
